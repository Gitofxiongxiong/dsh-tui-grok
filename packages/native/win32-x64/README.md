# `@dsh-pager-grok/native-win32-x64`

Prebuilt `dsh-pager` for Windows x64. Installed automatically as an optional
dependency of `@dsh-pager-grok/cli`. This package has no `bin` field; spawn
`bin/dsh-pager.exe` after resolving the package.
