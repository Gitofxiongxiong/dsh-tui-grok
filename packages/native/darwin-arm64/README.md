# `@dsh-pager-grok/native-darwin-arm64`

Prebuilt `dsh-pager` for macOS arm64. Installed automatically as an optional
dependency of `@dsh-pager-grok/cli`. This package has no `bin` field; spawn
`bin/dsh-pager` after resolving the package.
