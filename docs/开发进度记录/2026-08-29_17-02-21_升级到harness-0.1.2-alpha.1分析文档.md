# 升级到 Harness 0.1.2-alpha.1 分析文档

> 记录时间：2026-08-29 17:02:21 +0800
> 操作者/agent：主代理（分析）+ 用户确认
> 状态：completed

## 目标与背景

用户要求：基于 `deepseek-harness-版本差异分析.html`（0.1.0-rc.8 → 0.1.2-alpha.1，
141eb6fef8 → cd5ef81481，1286 commits），分析 dsh-tui-grok 项目要升级到最新
Harness 需要如何调整。只分析、不改代码；随后用户要求把分析整理成文档。

本批次产物是一份独立的升级分析文档（markdown），不改任何代码。

## 设计契约和复用依据

- 对应长期计划章节：无（纯分析文档，不涉及代码计划）；
- Grok source path、commit、SOURCE_REV：不涉及；
- 复用等级：不涉及。

分析对象（只读）：

- `/home/leo/aidreamschool/deepseek-harness`（旧，0.1.0-rc.8，141eb6fef8）；
- `/home/leo/aidreamschool/DeepSeek-Harness-latest`（新，0.1.2-alpha.1，cd5ef81481）；
- 本仓库 `/home/leo/aidreamschool/dsh-tui-grok`（HEAD 15fb12c）。

## 计划修改范围

- 文件：`docs/开发进度记录/2026-08-29_17-02-21_升级到harness-0.1.2-alpha.1分析文档.md`
  （本记录）、仓库根目录新增升级分析文档（命名与既有
  `deepseek-harness-版本差异分析.html` 平行）；
- 预期行为：文档完整记录现状接线、破坏面、方法映射、改造顺序、风险与验证；
- 不在范围内：任何代码、脚本、配置、依赖改动；后续实施必须新建记录。

## 风险、回滚和依赖

- 纯文档变更，无回滚风险；删除文档即可回滚；
- 分析结论基于两个检出只读比对，检出未被修改。

## 实际修改

- 文件：
  - 新增升级分析文档 `dsh-tui-grok-升级到harness-0.1.2-alpha.1分析.md`（仓库根目录，
    与既有 `deepseek-harness-版本差异分析.html` 平行）；
  - 本进度记录。
- 摘要：见分析文档正文。核心结论：Rust 侧线协议 v1 不变，破坏面集中在
  TS 插件层（ctx.apiProxy 删除 → 域控制器/Remote、流帧词汇翻译、waterfall
  审批应答）、依赖升版 0.1.2-alpha.1、code-mode→ptc、preset 目录迁移、
  apps/cli/lib/bin.js 需先构建、recovery 插件退役。

## 验证结果

- 命令：无编译/测试（纯文档批次）；
- 结果：分析文档与聊天中给出的结论一致；所有关键断言均已在两个检出中
  实测验证（方法映射、base patch 行 id 对比、包存在性、帧类型定义）。

## Git 提交

- commit message：`docs(analysis): 升级到 Harness 0.1.2-alpha.1 的 dsh-tui-grok 调整分析`
- Progress-Record trailer：`Progress-Record: docs/开发进度记录/2026-08-29_17-02-21_升级到harness-0.1.2-alpha.1分析文档.md`
- 暂存区审计：仅本记录 + 分析文档两个文件。

## 未解决问题和下一步

- 未决：session.list 行 agentPreset 补齐方案、history view 是否用
  agent-tool-presentation 重算，需在实施批次中决策；
- 下一步：按文档"改造工作量排序"分批次实施（每批先建新记录）。
