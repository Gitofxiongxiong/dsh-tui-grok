# M1 身份 API 校正

> 记录时间：2026-08-23 00:27:18 +0800
> 操作者/agent：Codex `/root`
> 状态：completed

## 目标与背景

移除 `DshRenderEntryId::source_seq()` 这个对 `Partial { step }` 做错误语义推断的未使用 API，避免把 partial step 当成源事件序号。

## 设计契约和复用依据

- 对应长期计划章节：M1.1/M1.2，稳定 identity 不得被数组索引或错误字段替代。
- Grok source：不涉及 vendor；DSH-neutral identity 适配位于 `crates/dsh-pager/src/presentation.rs`。
- 复用等级：C。

## 计划修改范围

- 文件：`crates/dsh-pager/src/presentation.rs`、本记录。
- 预期行为：删除错误的 convenience conversion；保留明确的 `DshSeq` 字段在 `TranscriptRow` 和 render entry `source_seq` 上。
- 不在范围内：协议、UI、vendor、fixture 和运行时行为。

## 风险、回滚和依赖

- 风险：外部调用者可能依赖这个尚未发布的 API；当前 workspace 无调用点。
- 回滚：回退本次单文件 commit；不触及已完成 M0/M1 主提交。

## 实际修改

- 删除 `DshRenderEntryId::source_seq()` 这个对 partial step 进行错误序号推断的 API。

## 验证结果

- `cargo fmt --all -- --check`：通过。
- `cargo check --workspace`：通过。
- `cargo test -p dsh-pager --locked`：通过。

## Git 提交

- commit message：`fix: remove ambiguous partial source sequence helper`。
- Progress-Record trailer：`docs/开发进度记录/2026-08-23_00-27-18_M1身份API校正.md`。
- 暂存区审计：只包含本记录和 `crates/dsh-pager/src/presentation.rs`。

## 未解决问题和下一步

无；完成后继续 M2。
