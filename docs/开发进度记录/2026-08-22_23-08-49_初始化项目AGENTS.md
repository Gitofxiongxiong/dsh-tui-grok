# 初始化项目 AGENTS.md

> 记录时间：2026-08-22 23:08:49 +0800  
> 操作者/agent：Codex  
> 状态：planned

## 目标与背景

在 successor 项目根目录建立项目级 AGENTS.md，使后续开发者和 agent 在修改
Grok Build TUI 适配代码前，能先看到本项目的架构、复用、进度记录、测试和
提交约束。

## 参考依据

- DeepSeek Harness 的通用协作约定：/home/leo/code/deepseek-harness/AGENTS.md；
- 本项目长期总控：docs/GROK_BUILD_TUI_DEEPSEEK_ADAPTATION_PLAN.md；
- 本项目架构、来源、测试和进度记录说明；
- 当前 Grok mirror commit：19d42e35c07a9c9244f03f6df0c4c353f970d4f9；
- 当前 Grok SOURCE_REV：7d67deacbeb1c1093fdb4f9bcbfab2630e18a6aa。

## 计划修改范围

- 新增：/home/leo/code/dsh-pager-grok/AGENTS.md；
- 收录：Grok-first 复用、DSH 真源边界、先写进度记录、一个记录对应一个
  Git commit、Rust 验证门禁和 vendor/license 规则；
- 不修改 Rust、协议、runtime、vendor 或其他业务文件。

## 预期验证

- AGENTS.md 位于项目根目录；
- 规则与长期主计划及进度记录 README 一致；
- 本文件和 AGENTS.md 的 Markdown 链接可解析；
- 代码验证不因本次文档初始化发生变化。

## 实际结果（2026-08-22 23:11:35 +0800）

- 新增：/home/leo/code/dsh-pager-grok/AGENTS.md；
- AGENTS.md 已包含项目目标、Grok-first 复用、DSH 真源边界、进度记录、
  一记录一 commit、架构所有权、验证门禁和安全边界；
- 本次没有修改 Rust、协议、runtime、vendor 或 Cargo.lock；
- AGENTS/docs Markdown 链接检查：通过；
- AGENTS.md：192 行；
- 当前项目仍没有 Git 元数据，因此没有创建 commit；Git 初始化属于 M0.1。

## 后续

任何下一次仓库修改必须先在 docs/开发进度记录 创建新的时间戳记录，并在
Git 根建立后按 Progress-Record trailer 建立一记录一 commit 的提交。
