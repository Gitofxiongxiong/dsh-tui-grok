# DSH 多版本文档对齐

> 记录时间：2026-08-30 13:14:54 +0800
> 操作者/agent：Codex
> 状态：completed

## 目标与背景

将已通过评审且 D1–D6 已确认的
`docs/DSH_MULTI_VERSION_COMPATIBILITY_PLAN.md` 与现有文档入口、历史方案和
当前行为说明对齐。本批只标注方案的规范性地位与被取代的历史设计，
不改写现有运行时行为陈述。

## 设计契约和复用依据

- 对应长期计划章节：`DSH_MULTI_VERSION_COMPATIBILITY_PLAN.md` §5–§7、§11、§19、D1–D6。
- Grok source path、commit、SOURCE_REV：不适用；本批不修改 Grok-derived 源码或 UI 行为。
- 复用等级 A0/A1/B/C/D：D（纯 DSH 适配架构文档，不引入 Grok runtime）。
- 保留 README 中关于当前 npm rc.8 和源码 alpha.1 的真实行为语句，仅补充已立项的 rc.2 迁移路线。

## 计划修改范围

- 主进度记录：`docs/开发进度记录/2026-08-30_13-14-54_DSH多版本文档对齐.md`
- 文件：`docs/README.md`
- 文件：`AGENTS.md`
- 文件：`docs/PRODUCT_PLUGIN_LAUNCHER.md`
- 文件：`docs/EXTERNAL_DSH_PLUGIN.md`
- 文件：`docs/ARCHITECTURE.md`
- 文件：`README.md`
- 文件：`docs/TESTING.md`
- 预期行为：文档索引和开工阅读入口可发现多版本总控方案；历史方案顶部明确时代与被取代决策；架构和测试文档指向新分层与矩阵。
- 不在范围内：任何源码、manifest、lockfile、CI、profile/script 行为，以及总控方案正文。

## 风险、回滚和依赖

- 风险：历史文档状态块若表述过度，可能让读者误以为当前运行时已经支持 rc.2。
- 缓解：明确使用“方案已立项/迁移路线”，不宣称 Phase 4/6 已完成。
- 回滚：单独 revert 本批文档 commit 即可，不影响运行时。
- 依赖：新增链接目标 `docs/DSH_MULTI_VERSION_COMPATIBILITY_PLAN.md` 已存在。

## 预期验证

- `git diff --check`
- `git diff --name-only` 确认只有本记录和上述文档。
- 逐一检查新增 Markdown 相对链接目标存在。
- 审阅 diff 确认未改动任何对当前真实行为的描述。

## 实际修改

- `docs/README.md`：在文档索引增加 DSH 多版本总控方案入口。
- `AGENTS.md`：将总控方案加入开工阅读清单，限定于 DSH 适配、桥接或协议边界工作。
- `docs/PRODUCT_PLUGIN_LAUNCHER.md`：顶部标注 rc.8 peer/单 runtime Bundle 为被 D1/D3 取代的历史方案。
- `docs/EXTERNAL_DSH_PLUGIN.md`：顶部标注其为 rc.8 Host ApiProxy 时代的 `apiproxy-v1` 恢复基线。
- `docs/ARCHITECTURE.md`：增加“TypeScript server 边界”，概述 core、SPI、adapter 与 profile/runtime 的责任。
- `README.md`：保留当前 alpha.1 精确版本描述，补充 rc.2 npm 支持已进入迁移路线。
- `docs/TESTING.md`：增加指向总控方案 §16–§17 的测试矩阵入口。
- 未修改任何源码、manifest、lockfile、CI 或总控方案正文。

## 验证结果

- `git diff --check`：通过，无空白或 patch 格式错误。
- `git diff --stat` / `git status --short`：除本记录外只有计划列出的 7 份文档，为纯文档 diff。
- `test -f docs/DSH_MULTI_VERSION_COMPATIBILITY_PLAN.md`：通过；本批新增相对链接均指向该已存在文件，其章节标题与链接锚点一致。
- diff 人工审阅：README 对 npm rc.8 和当前源码 alpha.1 的现实陈述均保留，未将 rc.2 写成已支持。

## Git 提交

- commit message：`docs(compat): align multi-version architecture docs`
- Progress-Record trailer：`Progress-Record: docs/开发进度记录/2026-08-30_13-14-54_DSH多版本文档对齐.md`
- 暂存区审计：提交前使用 `git diff --cached --name-only` 核对为本记录和上述 7 份文档。

## 未解决问题和下一步

- 本批无阻塞。历史文档正文未逐段改写，以顶部状态块和总控方案为规范性入口。
- 下一步是 Phase 0：建立 support registry、检查脚本与基线清单；将使用新进度记录和独立 commit。
