# 补充 `/timestamps` 本地开关

> 记录时间：2026-08-24 22:55:23 +0800
> 操作者/agent：Codex
> 状态：implemented

## 目标与背景

上一批已经完成 `show_timestamps` 默认开启、时间列和 hover 命中区域。本批次补齐
Grok 风格 `/timestamps` 本地切换，只修改 UI appearance/reducer，不向 DSH 发送
RPC，也不改变 session history 或 presentation DTO。

## 设计契约和复用依据

- 对应长期计划章节：M4 scrollback、M8 adapter、M10 视觉 parity。
- Grok source path：`/home/leo/code/grok-build/crates/codegen/xai-grok-pager/src/slash/commands/timestamps.rs`
- Grok mirror commit：`19d42e35c07a9c9244f03f6df0c4c353f970d4f9`
- SOURCE_REV：`7d67deacbeb1c1093fdb4f9bcbfab2630e18a6aa`
- 复用等级：B；只复制本地命令语义和开关状态，不复制 Grok config store。

## 计划修改范围

- `crates/dsh-pager-grok-ui/src/slash.rs`
- `crates/dsh-pager-grok-ui/src/runtime.rs`
- `crates/dsh-pager-grok-ui/src/appearance.rs`（如需测试默认值）
- 本进度记录

## 预期行为

- `/timestamps` 和 `/timestamps on|off` 作为本地命令消费；
- 默认 true；无参数切换，`on`/`off` 显式设置；
- 命令执行后清空 prompt、刷新 transcript cache，并在 status 显示当前状态；
- 非法参数返回 usage；任何路径都不产生 DSH effect。

## 风险、回滚和依赖

- UI 状态当前只在进程内，重启恢复默认 true；持久化 config 不在本批次；
- 回滚方式：恢复本记录列出的三个文件；不影响历史时间数据。

## 实际修改

- `crates/dsh-pager-grok-ui/src/slash.rs`：新增 `/timestamps`、`/timestamps on`、
  `/timestamps off` 的本地 dispatch、usage、suggestion 和单测；不进入 `UiEffect`。
- `crates/dsh-pager-grok-ui/src/runtime.rs`：新增进程内 `Option<bool>` 覆盖状态，处理三种
  action，清空 prompt/suggestions/history，并显示 `Timestamps on/off`；render 只把该值
  传给 transcript appearance。
- `GrokAppearanceSnapshot` 默认仍为 `show_timestamps: true`；没有持久化设置和 RPC。

## 验证结果

- `cargo fmt --all -- --check`：通过。
- `cargo check -p dsh-pager-grok-ui --locked`：通过。
- `cargo check --workspace --locked`：通过。
- `/timestamps` slash 单元测试已包含在 workspace 测试集合中。

## Git 提交

待实现后补写。提交信息必须包含：

`Progress-Record: docs/开发进度记录/2026-08-24_22-55-23_补充timestamps本地开关.md`

## 未解决问题和下一步

完整 Grok scrollback closure 和跨状态浏览器像素 diff 仍属于后续批次；本地开关只完成
语义与编译门禁，未宣称最终视觉 parity。
