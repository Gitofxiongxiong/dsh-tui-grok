# DSH alpha.1 升级分析校正

> 记录时间：2026-08-29 18:49:45 +0800
> 操作者/agent：Codex
> 状态：completed

## 目标与背景

校正 `dsh-tui-grok` 从 DeepSeek Harness `0.1.0-rc.8` 升级到
`0.1.2-alpha.1` 的分析文档。原文已识别 ApiProxy 删除和 Typert
Remote 迁移，但对 prompt `requestId`、history `throughSeq`/压缩
chunk、Host-local tool view 重算、Dashboard cold peek 缺口以及实际
改动规模的描述不完整。

## 设计契约和复用依据

- 对应长期计划章节：2.3 DSH 真源、2.4 Intent/Effect/Receipt 边界、3.2 目标数据流、M1 contract/projection/effects。
- Grok source path、commit、SOURCE_REV：不适用；本批次只校正 DSH Host 升级分析，不修改 Grok-derived 源码。
- 复用等级 A0/A1/B/C/D：C（DSH 协议、Host 服务与运行时接缝分析）。

## 计划修改范围

- 文件：
  - `dsh-tui-grok-升级到harness-0.1.2-alpha.1分析.md`
  - `docs/开发进度记录/2026-08-29_18-49-45_DSH-alpha1升级分析校正.md`
- 预期行为：形成字段级契约准确、可按阶段执行的大型 Host 集成层改造方案。
- 不在范围内：不修改 TypeScript/Rust 实现、profile、依赖锁文件、fixture 或测试。

## 风险、回滚和依赖

- 风险：方法名对应正确但请求/响应字段或流生命周期描述错误，会误导后续实现。
- 缓解：以两个 Harness checkout 和当前 Rust/TypeScript 消费代码逐项交叉核对。
- 回滚：文档独立变更，可通过反向文本补丁回滚；不影响运行时。
- 依赖：`/home/leo/aidreamschool/deepseek-harness` 和 `/home/leo/aidreamschool/DeepSeek-Harness-latest` 的当前源码。

## 实际修改

- 文件：`dsh-tui-grok-升级到harness-0.1.2-alpha.1分析.md`
- 摘要：
  - 将规模结论从“Rust 几乎不动”校正为“大型 Host 集成层适配，但不是全项目重写”。
  - 补充 `session.prompt`/`subagent.prompt` 必填 `requestId` 和 Rust protocol/loader/effects 影响。
  - 补充 `follow` opening snapshot、`page.address/throughSeq`、chunk run 展开、history/live barrier 和 follower generation 生命周期。
  - 记录 Dashboard cold peek 在新 API 中没有有界、不激活等价物，区分短期 `inspect` 与长期 cold-tail 上游能力。
  - 校正 tool view 来源：使用 Host-local `ctx.tools` 的 `presentCall/presentResult`，而非 `agent-tool-presentation`。
  - 补充 `modelSelection`/`agentPreset` projection 展平、workspace baseline、waterfall 合成 ID、统一 `TypertRemoteFailure` 映射。
  - 增加 `TuiHarnessBridge` 目标边界和六阶段可验证实施顺序。
  - 校正 telemetry/session-log 与 JSONL/SQLite session-query 的表述。
- 文件：`docs/开发进度记录/2026-08-29_18-49-45_DSH-alpha1升级分析校正.md`
- 摘要：回填本批次范围、实际修改和验证证据。

## 验证结果

- 命令：`git diff --check`
- 结果：通过，无空白或补丁格式错误。
- 命令：搜索原文中的过期结论关键词（`43 个`、`agent-tool-presentation 重算`、`ctx.remote.$on`、`SQLite schema 19` 等）。
- 结果：过期表述已清理；“不是零改动”仅作为校正后结论保留。
- 命令：逐项交叉核对新版 `session-controller`、`history`、`subagent`、`workspace-controller`、`agent-presets`、`tools`、`typert-protocol` 源码和当前 Rust/TS 消费代码。
- 结果：文档已补齐本批次识别的字段级破坏和生命周期约束。
- 说明：本批次只修改文档，未运行编译、单测或真实 Harness E2E。

## Git 提交

- commit message：`docs: correct DSH alpha1 upgrade analysis`
- Progress-Record trailer：`Progress-Record: docs/开发进度记录/2026-08-29_18-49-45_DSH-alpha1升级分析校正.md`
- 暂存区审计：只包含本记录和 `dsh-tui-grok-升级到harness-0.1.2-alpha.1分析.md`；现有未跟踪 HTML 和另一份历史分析记录不纳入本提交。

## 未解决问题和下一步

- 实现工作不在本文档批次内；开始实现前另建进度记录。
- Dashboard cold-tail/cursor 仍是上游能力缺口；实现批次需决定是否先接受全日志 `inspect` 临时方案。
