# Phase 6 发布前 CI 锁文件修复

> 记录时间：2026-08-30 18:00:09 +0800
> 操作者/agent：Codex
> 状态：completed

## 目标与背景

- 正式发布授权后已 push 当前分支并创建 PR #4；在任何 npm 上传前等待远端 CI。
- CI run `33305278743` 的 `verify-ts + contracts (ubuntu)` 在 clean runner 执行
  `pnpm install --frozen-lockfile` 时失败：CLI manifest 新增的五个 exact `0.2.0`
  native optional dependencies 不在 lock importer 中。
- 本机既有 workspace link 状态曾让同一 frozen 命令通过；以 clean runner 结果为准，
  修正 lockfile 并增加干净环境复核证据。

## 计划修改范围

- `pnpm-lock.yaml`：只补 `packages/dsh-pager-cli` importer 的五个 optional dependency
  specifier/link resolution。
- 本进度记录：记录 CI 失败、生成方式、验证和 PR 更新。
- 不修改 package/runtime、Rust wire、协议、UI 或 registry 支持状态。

## 风险与回滚

- 只允许 lock importer 与 CLI manifest 精确一致；不得重解整个依赖图或改变已审计
  package versions。
- 先在临时 clean clone 中生成/验证差异，再以最小 patch 落仓库；修复失败可单独
  revert 本 commit。
- npm publish、dist-tag、Tag、Release 继续保持未执行；PR CI 全绿前不合并。

## 预期验证

- 临时 clean clone 与当前 workspace 的 `pnpm install --frozen-lockfile` 均通过。
- `pnpm run verify:ts`、support/pack 相关门禁保持通过。
- push 新 commit 后 PR #4 全部 required checks 和三版本矩阵通过。

## 实际修改与验证结果

- 在临时干净 clone `/home/leo/code/dsh-pager-lockfix.XVUGuU` 中使用与 CI
  相同的 `CI=true` 重现了 `ERR_PNPM_OUTDATED_LOCKFILE`；差异精确指向
  `packages/dsh-pager-cli/package.json` 的五个 `0.2.0` native optional
  dependencies。
- pnpm 对 workspace optional dependencies 解析为 sibling `link:`，因此仅在
  `packages/dsh-pager-cli` importer 补入五个 `specifier: 0.2.0` 与对应
  `link:../native/*`；未重解其他依赖。
- 下列两个目录均通过严格锁文件安装：
  - 当前 workspace：
    `CI=true corepack pnpm@11.20.0 --pm-on-fail=ignore install --frozen-lockfile --ignore-scripts`
  - 干净 clone：
    `CI=true corepack pnpm@11.20.0 --pm-on-fail=ignore install --frozen-lockfile --ignore-scripts`
- `node scripts/verify-cli-pack.mjs`：通过，产物
  `dsh-pager-grok-cli-0.2.0.tgz` 不含 workspace/link 泄漏。
- `corepack pnpm@11.20.0 --pm-on-fail=ignore run verify:ts`：通过；其中
  protocol `17/17`、server `77/77`、embedded `3/3`、两个 runtime 各
  `1/1`、CLI `23/23`，runtime/CLI/native pack 门禁全部通过。
- `DSH_CHECKOUT_RC8=/home/leo/code/deepseek-harness \
  DSH_CHECKOUT_RC2=/home/leo/code/deepseek-harness-dsh-v0.1.1-rc.2 \
  DSH_CHECKOUT_ALPHA=/home/leo/code/deepseek-harness-latest \
  node scripts/check-dsh-support.mjs`：通过，三个精确版本和生成文档一致。
- `git diff --check`：通过。
- 本批未执行 npm publish/dist-tag、Git tag/Release 或 PR merge；推送后
  由 PR #4 重跑远程全矩阵。

## Git 提交

- 预计 commit message：`fix(ci): restore CLI optional dependency lock entries`
- Progress-Record trailer：
  `Progress-Record: docs/开发进度记录/2026-08-30_18-00-09_Phase6发布前CI锁文件修复.md`

## 未解决问题和下一步

- PR #4 全矩阵通过前不合并。
- npm OIDC 可以发布已存在且已配置 trusted publisher 的包，但新包
  `@dsh-pager-grok/runtime-apiproxy-v1` 必须先用有权凭据完成首次发布，然后才能
  在 npm 上配置 GitHub Actions trusted publisher；当前本机 `npm whoami` 为
  `E401`，因此该认证/bootstrap 仍是独立发布阻塞。
