# v0.2.0 发布收尾延期

> 记录时间：2026-08-30 21:59:08 +0800
> 操作者/agent：Codex
> 状态：completed

## 目标与背景

- 按维护者决定停止今天剩余的 npm dist-tag 授权与验证，不继续创建 GitHub Release。
- 将 `v0.2.0` 已完成事实、当前精确 dist-tag 状态和后续逐项执行顺序登记为可恢复 TODO。
- 通过普通文档分支、GitHub Pull Request 和 rebase merge 提交记录；本批不执行任何
  `npm publish`、`npm dist-tag`、workflow dispatch、版本 Tag 变更或 GitHub Release。

## 设计契约和复用依据

- 对应长期计划章节：`docs/DSH_MULTI_VERSION_COMPATIBILITY_PLAN.md` §17.2、§19 Phase 6；
  `docs/GIT_PR_RELEASE_WORKFLOW.md` §7.5–§7.6、§8。
- 发布物与 `v0.2.0` Tag 不可变；后续只移动 dist-tag 并做只读 registry 验证，不覆盖或
  重发任何公开 version。
- npm 每个 package 的 dist-tag 写操作分别触发维护者网页认证；后续必须严格串行执行，
  每次完成并核验一个 package 后才开始下一个。
- Grok source path、commit、SOURCE_REV：不涉及 Grok 源码。
- 复用等级 A0/A1/B/C/D：不适用；本批仅更新发布操作记录。

## 计划修改范围

- 文件：`docs/RELEASE_CANDIDATE_CHECKLIST.md`。
- 文件：本进度记录。
- 预期行为：新增延期 TODO，记录已完成正式发布 workflow、当前 dist-tag 分布、剩余三个
  `latest` 移动、最终核验和 GitHub Release 步骤。
- 不在范围内：代码、workflow、package manifest、lockfile、支持注册表、npm registry
  写操作、Git Tag、GitHub Release。

## 风险、回滚和依赖

- 风险：把部分完成的 dist-tag 状态误写为全量完成，或让后续维护者重复执行已成功操作。
- 控制：以本批开始前的逐包 `npm view ... dist-tags --json` 结果为事实来源，TODO 只列尚未
  完成的三个 package；保留 `release-candidate=0.2.0` 作为无害别名。
- 回滚：文档 PR 可通过后续文档 commit 更正；不修改已发布 package、Tag 或 Release。
- 依赖：后续执行需要 npm 维护者网页认证；GitHub Release 只能在全部默认 tag 核验后创建。

## 实际修改

- 文件：`docs/RELEASE_CANDIDATE_CHECKLIST.md`。
  - 将正式 Trusted Publishing/recovery workflow 标记为已完成，但保留 dist-tag 与
    GitHub Release 两项未勾选。
  - 登记 recovery run `33313278488` 的 11/11 全绿事实和一次性 runtime bootstrap
    provenance 例外。
  - 登记暂停时七个 package 的精确 dist-tag 分布；已完成四项，剩余 CLI、Darwin ARM64
    与 Windows x64 三项。
  - 新增严格串行的后续 TODO：CLI 优先，每项网页认证后立即只读验证；最终全包核验、
    registry smoke、GitHub Release 与单独 post-release 记录。
  - 明确保留 `release-candidate=0.2.0` 无害别名，避免七次非必要删除认证。
- 文件：本进度记录。
- 本批未改动代码、workflow、manifest 或 lockfile，未运行 npm registry 写操作、workflow
  dispatch、Tag 变更或 GitHub Release。

## 验证结果

- `npm view '<package>' version dist-tags --json`（七个公开 package，逐项只读）：通过。
  - `latest=0.2.0`：Linux x64、Linux ARM64、Darwin x64、runtime；
  - `latest=0.1.0` 且 `release-candidate=0.2.0`：CLI、Darwin ARM64、Windows x64。
- `git diff --check`：通过，无空白错误。
- `gh auth status`：通过，GitHub CLI 登录有效，可提交普通文档分支和 PR。
- `git status --short --branch`：确认已有 `package.json` 修改未被本批触碰，精确暂存时排除。
- 代码/PTY/真实 DSH：未运行；本批只有发布事实与 TODO 文档，不改变运行时或 TUI。

## Git 提交

- commit message：`docs(release): defer v0.2.0 tag promotion`。
- Progress-Record trailer：
  `Progress-Record: docs/开发进度记录/2026-08-30_21-59-08_v0.2.0发布收尾延期.md`
- 暂存区审计：提交前只暂存本记录与 `docs/RELEASE_CANDIDATE_CHECKLIST.md`；
  `package.json` 明确排除。

## 未解决问题和下一步

- `@dsh-pager-grok/cli`、`@dsh-pager-grok/native-darwin-arm64` 与
  `@dsh-pager-grok/native-win32-x64` 的 `latest` 仍为 `0.1.0`；按 checklist 严格串行
  提升并逐项验证。
- 全部 `latest=0.2.0` 且 registry smoke 通过后才能创建 `v0.2.0` GitHub Release。
- 完成上述事项时新建独立 post-release 进度记录，回写最终事实并勾选剩余 checklist；
  不复用本记录追加新的 registry 写操作。
