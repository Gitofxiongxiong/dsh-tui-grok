# DSH 多版本 Phase 3 前置真实 E2E 基线

> 记录时间：2026-08-30 14:15:17 +0800
> 操作者/agent：Codex
> 状态：blocked

## 目标与背景

在 Phase 3 重构前恢复 DSH 0.1.2-alpha.1 的真实 E2E/PTY 基线，为已审核的
Phase 1 与 Phase 2 补齐先前因 profile 未初始化而缺失的动态证据。只有本批真实
门禁全绿才开始 controllers-v2 拆分。

关联既有记录：

- `docs/开发进度记录/2026-08-30_13-23-04_DSH多版本Phase1后端抽取.md`
- `docs/开发进度记录/2026-08-30_14-00-47_DSH多版本Phase2Core契约测试.md`

## 设计契约和复用依据

- 对应长期计划章节：§16.3、§16.4、§19 Phase 1–3。
- D-R1：允许且要求使用仓库 `scripts/setup-dev-profile.sh` 初始化项目专用 profile；
  不删除/覆盖任何既有 profile、sessions 或 credentials，已有目标必须先改名备份。
- profile 名：`dsh-pager-grok-e2e`。
- alpha.1 checkout：`/home/leo/code/deepseek-harness-latest`，HEAD
  `cd5ef8148158c3a752a658978873241fdf8e2bbc`，与 support registry 一致。
- 复用等级：A0（只运行既有 setup/E2E/PTY 门禁，不改变产品实现）。

## 计划修改范围

- 仓库文件：仅本进度记录。
- 机器状态：新建 `/home/leo/.dsh/profiles/dsh-pager-grok-e2e`，内容由仓库
  `setup-dev-profile.sh` 和 alpha.1 CLI 生成。
- 临时状态：`real-e2e.sh` 自建并清理 `/tmp/dsh-pager-real-e2e.*`；独立 full PTY
  使用 mock backend，不写真实 profile。
- 预期行为：建立可重复的 alpha.1 hello/list/dashboard/load/PTY 基线。
- 不在范围内：其他 profile、`/home/leo/.dsh/sessions`、credentials、方案正文、
  任何代码/manifest/lockfile、Phase 3 实现。

## 风险、回滚和依赖

- 修改前探测：目标 profile 路径不存在，因此无需改名备份；不会设置
  `DSH_TUI_PROFILE_ALLOW_UPDATE=1`。
- setup 会构建本地 TS 包并通过 DSH CLI 新建专用 profile，可能访问 package
  manager；失败时保留目录供诊断，不递归删除。
- real E2E 默认 `--new` 会在当前 DSH_HOME 新建测试 session；不删除或覆盖用户
  既有 profile/session/credentials。PTY credential 使用脚本创建的临时 DSH_HOME，
  并由脚本清理。
- 回滚：本批不自动删除机器 profile；如维护者需要清理，应先人工核对后改名归档。
- 依赖：Node/pnpm、Rust toolchain、alpha.1 已构建 checkout、项目 setup/E2E 脚本。

## 实际修改

- 仓库内仅新增本进度记录；未修改产品代码、manifest、lockfile、方案正文或测试。
- 修改机器状态前确认目标路径不存在，随后使用既有 setup 脚本创建了
  `/home/leo/.dsh/profiles/dsh-pager-grok-e2e`；没有覆盖或改名任何既有 profile，
  也没有删除 sessions/credentials。setup 完整构建 protocol/server/embedded/runtime
  并成功写入 alpha.1 profile。
- 两次 `real-e2e.sh` 均通过 Rust 构建、真实 `hello`、`list`、`dashboard` 和
  `load-only --new`；失败点均为脚本最后的隔离 DSH_HOME 真实后端 PTY，错误同为
  `RuntimeError: timed out loading backend session`。
- 第二次仅把 `REAL_E2E_TIMEOUT` 从默认 45 秒放宽到 90 秒，用于排除冷启动时延；
  仍在同一阶段失败。依照“同一问题连续两次无法推进即停止”的约束，不做第三次
  尝试，不运行不能替代真实门禁的 mock PTY，也不开始 Phase 3a。
- 本批没有为绕过门禁修改 profile、E2E 脚本、超时默认值或产品实现。

## 验证结果

- `node scripts/check-dsh-support.mjs`：通过；注册表结构、runtime 精确版本约束通过，
  本次未注入 checkout 环境变量，因此脚本按设计打印 checkout 校验 skip。
- `DSH_HARNESS_ROOT=/home/leo/code/deepseek-harness-latest DSH_TUI_PROFILE=dsh-pager-grok-e2e scripts/setup-dev-profile.sh`：
  通过，退出码 0；专用 profile 创建成功。
- `DSH_HARNESS_ROOT=/home/leo/code/deepseek-harness-latest DSH_TUI_PROFILE=dsh-pager-grok-e2e scripts/real-e2e.sh`：
  失败，退出码 1；真实 hello/list/dashboard/load-only 均通过，默认 45 秒真实 PTY
  等待 `SessionLoaded` 超时。
- `DSH_HARNESS_ROOT=/home/leo/code/deepseek-harness-latest DSH_TUI_PROFILE=dsh-pager-grok-e2e REAL_E2E_TIMEOUT=90 scripts/real-e2e.sh`：
  失败，退出码 1；前置真实检查再次通过，真实 PTY 在 90 秒后以同一错误退出。
- `python3 scripts/pty-smoke.py --binary target/debug/dsh-pager --full`：未运行；mock
  backend 不能满足本批“真实 E2E + PTY”前置条件，且第二次同类失败后必须停止。
- Phase 1/2 补充证据：hello/list/dashboard/load-only 已获得两轮真实 alpha.1 证据；
  PTY 证据未补齐，因此不能宣称真实基线全绿。

## Git 提交

- commit message：`test(e2e): record blocked alpha controllers baseline`。
- Progress-Record trailer：
  `Progress-Record: docs/开发进度记录/2026-08-30_14-15-17_DSH多版本Phase3前置真实基线.md`
- 暂存区审计：提交前仅暂存本记录；不包含 setup 构建产物或机器 profile。

## 未解决问题和下一步

- 阻塞：隔离 DSH_HOME 中的真实后端在 45 秒和 90 秒两次尝试里都没有进入
  `SessionLoaded`。非交互真实调用正常，故当前证据把问题限定在真实 PTY 的隔离
  profile/session 启动链路，但尚不足以判断是 profile 复制、runtime 启动还是事件
  送达环节。
- §19 Phase 3 前置门禁：**未满足**。Phase 3a/3b 以及依赖它们的 Phase 4–6
  均未开始，不能标记完成。
- 维护者后续建议：在不改用户 profile 的临时 DSH_HOME 中保留失败现场并增加启动
  日志，定位 `SessionLoaded` 未到达的首个边界；修复后先以原始 45 秒命令复验，
  全绿后再恢复 Phase 3a。
- 专用 profile 保留在 `/home/leo/.dsh/profiles/dsh-pager-grok-e2e` 供后续诊断；
  本批不自动删除或改名归档。
