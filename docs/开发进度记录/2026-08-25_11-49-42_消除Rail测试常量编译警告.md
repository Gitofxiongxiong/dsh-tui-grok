# 消除 Rail 测试常量的普通编译警告

> 记录时间：2026-08-25 11:49:42 +0800
> 操作者/agent：Codex `/root`
> 状态：completed
> 归档说明（2026-08-25）：本文件是 `02a71be` 整合提交前的历史执行附件，不是独立
> commit authority；提交范围与 trailer 以
> `2026-08-25_15-26-51_S3R-S5上游闭包整合.md` 为准，以下阶段性原文保留。

## 目标与背景

`WAVE_FRAME_MS` 只被 Rail 波形平滑度单元测试使用，却定义在普通 production
编译配置中，导致 `cargo build` 报 `dead_code` warning。构建本身成功，但开发启动
日志不应携带这一条可消除的警告。

## 设计契约和复用依据

- 对应长期计划：2.1 视觉契约、测试/构建门禁；不改变 Rail 公式或终端呈现。
- Grok source/revision：不涉及新的 Grok 源码复用；沿用当前 Rail B 类适配。
- DSH-neutral seam、DTO/Intent/Effect、稳定 identity/generation：全部不变。
- 修复方式：把只服务测试的 `WAVE_FRAME_MS` 限定为 `#[cfg(test)]`，测试仍共享
  一个有名字的 33ms 采样契约，production library 不再编译该符号。

## 计划修改范围

- `crates/dsh-pager-grok-ui/src/views/transcript.rs`
- 本进度记录

不在范围内：修改 Rail 速度、包络、周期、刷新间隔或视觉样式；处理独立 Rail
时域审计提出的后续问题。

## 风险、回滚和依赖

- 风险极低：仅编译配置边界；若非测试 production 代码后来使用该常量，编译会
  明确失败而不是静默改变行为。
- 回滚：移除 `#[cfg(test)]` 即恢复原状。
- 当前有一个外部只读 Rail audit 正在运行；其说明明确禁止修改文件，不拥有本批
  变更。本批不修改其临时产物。

## 预期验证

- `cargo build -p dsh-pager-bin --locked`，确认 warning 消失
- `cargo test -p dsh-pager-grok-ui rail_wave_uses_a_long_gray_ramp_instead_of_chunky_jumps --locked`
- `cargo check --workspace --locked`
- `cargo clippy -p dsh-pager-grok-ui --all-targets --no-deps --locked -- -D warnings`
- `cargo fmt --all -- --check`
- `git diff --check`

## 实际修改

- 给只被 Rail 平滑度单元测试引用的 `WAVE_FRAME_MS` 添加 `#[cfg(test)]`。
- production library 不再编译该符号；测试配置仍使用同一个命名常量表达 33ms
  帧采样契约。Rail 速度、包络、周期和 runtime poll 均未改变。

## 验证结果

- `cargo build -p dsh-pager-bin --locked`：通过；重新编译 UI/bin，日志中没有
  `WAVE_FRAME_MS is never used` 或其他 warning。
- `cargo test -p dsh-pager-grok-ui rail_wave_uses_a_long_gray_ramp_instead_of_chunky_jumps --locked`：
  通过，1 passed。
- `cargo check --workspace --locked`：通过。
- `cargo clippy -p dsh-pager-grok-ui --all-targets --no-deps --locked -- -D warnings`：
  通过。
- `cargo fmt --all -- --check`：通过。
- `git diff --check`：通过。
- 本批没有用户可见 TUI 变化，只收紧测试符号的编译配置，因此不触发新的
  ttyd/Playwright 像素对照；上一批 Tool glyph 截图证据不受影响。

## Git 提交

- commit message：待用户要求；本批次默认不主动提交。
- Progress-Record trailer：
  `Progress-Record: docs/开发进度记录/2026-08-25_11-49-42_消除Rail测试常量编译警告.md`
- 暂存区审计：未暂存、未提交；工作区只包含本记录和 transcript 的一行
  `#[cfg(test)]` 修改。

## 未解决问题和下一步

- 本批范围内无未解决问题。
- 独立 Rail 时域审计仍为只读外部任务；其可能提出的功能调整应另建进度记录，
  不与本次编译 warning 修复合并。
