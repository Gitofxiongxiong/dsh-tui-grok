# Scrollback 上游优先路线纠偏

> 记录时间：2026-08-25 14:04:18 +0800
> 操作者/agent：Codex
> 状态：completed
> 归档说明（2026-08-25）：本文件是 `02a71be` 整合提交前的历史执行附件，不是独立
> commit authority；提交范围与 trailer 以
> `2026-08-25_15-26-51_S3R-S5上游闭包整合.md` 为准，以下阶段性原文保留。

## 目标与背景

- 纠正 `GROK_SCROLLBACK_CLOSURE_PLAN.md` 将当前 S2–S4 小型抽取实现标为
  “已完成”的错误基线。
- 把后续执行原则收紧为：优先复制 Grok Build 原文件、目录结构和上游测试，
  DSH 只在明确的 host/DTO/tick 接缝做适配，不再从上游文件中重写一个同职责的
  mini renderer。
- 保留已经正确闭合的 S1 行波/混色工作；当前 S2–S4 代码只作为迁移过渡路径，
  不能作为继续扩写 S5 的最终基础。

## 设计契约和复用依据

- 对应长期计划章节：`GROK_SCROLLBACK_CLOSURE_PLAN.md` 第 3、6、8、11、12 节。
- Grok source path：`/home/leo/aidreamschool/grok-build/crates/codegen/`
- mirror commit：`19d42e35c07a9c9244f03f6df0c4c353f970d4f9`
- SOURCE_REV：`7d67deacbeb1c1093fdb4f9bcbfab2630e18a6aa`
- 复用等级：A0/A1 为默认；B 只允许发生在 DSH-neutral seam，且必须保留上游
  组件边界、行为分支、不变量和测试矩阵；C 仅用于 DSH 真源。

## 计划修改范围

- 文件：
  - `docs/GROK_SCROLLBACK_CLOSURE_PLAN.md`
  - 本进度记录
- 预期行为：只修改长期计划和审计结论，不改变生产代码或用户可见 TUI。
- 不在范围内：S2R–S4R 代码重迁、vendor/source manifest、Cargo 文件、测试
  fixture、截图和 Git commit。

## 风险、回滚和依赖

- 风险：已有 S2–S4 进度记录声称“完成”，若只改计划而不注明审计依据，会造成
  两份文档互相矛盾。
- 处置：历史记录不删除、不静默改写；在新计划中明确把旧结论降级为“核心纵切
  已接入、闭包未完成”，并列出可以复核的结构差异。
- 回滚：本批只有文档变更，可按本记录所列两个文件逐项回退；不使用 destructive
  Git 命令。

## 预期验证

- `git diff --check`
- 人工核对计划中的路径、行数和测试数量与当前工作树/固定上游一致。
- 确认本批没有修改生产代码。

## 实际修改

- `docs/GROK_SCROLLBACK_CLOSURE_PLAN.md`
  - 将复用默认值明确为“复制固定上游原文件、目录边界和测试，再做边界适配”。
  - 新增 upstream-first 六步执行法，禁止把小型抽取放入 `vendor/grok/` 后冒充
    原文件闭包。
  - 记录当前 EntryRenderer 与 thinking/user/agent blocks 的本地/上游行数、测试数
    差距，以及 appearance、projected entry、timestamp、padding、pending、
    `skip_rows` 和全历史物化缺口。
  - 把“S2–S4 已完成”更正为“核心纵切已接入、闭包未完成”，保留原历史记录并
    由本次后续审计覆盖其完成结论。
  - 重排为 S2R appearance 类型树、S3R 完整 renderer/layout、S4R 完整 blocks/
    projected entry、S6A host range API、S6B 窗口 Buffer 绘制；规定 S2R–S4R
    完成前不得进入 S5。
  - 扩充完成门禁和 TEMPORARY 清单。
- 本进度记录：回写实际范围、验证结果和下一步。

## 验证结果

- `git diff --check`：通过。
- 人工复核固定上游与当前工作树：
  - EntryRenderer：182 / 1954 行，1 / 32 个测试；
  - thinking：204 / 738 行，2 / 7 个测试；
  - user：113 / 1166 行，1 / 41 个测试；
  - agent：35 / 492 行，1 / 11 个测试。
- `git status --short` 复核：本批只新增本记录并修改闭包计划；其余 S1–S4 脏
  工作树内容均为批次开始前已有，本批未改生产代码。
- 本批无用户可见 TUI 变更，因此不触发浏览器截图门禁；未运行 Rust 测试。

## Git 提交

- 本批不主动提交。
- 预期 trailer：
  `Progress-Record: docs/开发进度记录/2026-08-25_14-04-18_scrollback上游优先路线纠偏.md`

## 未解决问题和下一步

- 当前 S2–S4 production 仍走 mini extracts；路线已经纠偏，代码尚未完成重基。
- 下一批应单独建立进度记录，从 S2R 开始：复制上游 appearance scrollback
  runtime 类型树，定义 host resolved-value 构造 seam；之后依次执行 S3R、S4R。
- S2R–S4R 未完成前，不在当前 mini EntryRenderer 上继续实现 S5 tool/grouping。
