# 产品插件启动器方案文档

> 记录时间：2026-08-26 18:40:48 +0800
> 操作者/agent：main agent
> 状态：completed

## 目标与背景

用户要求：把「树外 DSH 插件、易安装、快速启动」的方案写成文档供审核，
并在官方 DSH 插件机制与本地代码的二次复审后，把更收敛的产品分发方案写回同一份文档。
硬约束：代码不得进入官方 `deepseek-harness`；不能宣称官方 `dsh tui`。

本批次只落方案文档，不改代码、协议、测试或 vendor。推荐方案保留
Rust pager 拥有 TTY 的双进程边界，把分发收敛为私有精确版本 DSH、单一 runtime
Bundle 和 npm 平台原生包。

## 设计契约和复用依据

- 对应计划：继续使用官方 `dsh plugin --profile ... add ...` 管理 Bundle；产品入口改为携带精确版本 DSH/pnpm 的独立启动器，双进程 TTY 仍由 Rust pager 持有。
- Grok source path、commit、SOURCE_REV：不适用（不复制 Grok 源码）。
- 复用等级：不适用。

## 计划修改范围

- 文件：
  - `docs/PRODUCT_PLUGIN_LAUNCHER.md`（新增，方案正文）
  - `docs/README.md`（增加文档入口链接）
  - 本进度记录
- 预期行为：可直接审阅安装/启动/发布/PR 切片；正文明确区分「当前推荐 v2」与「原 v1 审计基线」；不改变运行时。
- 不在范围内：不实现启动器、不 npm publish、不改 Cargo / TypeScript 包。

## 风险、回滚和依赖

- 风险：方案与代码事实不符 → 已对照 `dsh-tui-grok`、官方 CLI/profile/plugin 源码、官方文档与 `dsh-TUI/bin/dsh-tui.js` 核实。
- 风险：推荐 v2 尚未做干净 npm 安装 spike → 文档将「私有 pnpm `.bin` 临时注入 PATH」与平台包安装列为 PR 硬门禁，不宣称已实现。
- 回滚：删除 `docs/PRODUCT_PLUGIN_LAUNCHER.md` 并还原 `docs/README.md`。
- 依赖：无运行时依赖。

## 实际文件与摘要

- 新增 `docs/PRODUCT_PLUGIN_LAUNCHER.md`；在原 v1 草案前增加当前推荐 v2 架构、官方机制依据、冷/热路径、发布顺序、风险和替代 PR 计划。
- `docs/README.md` 增加该文档链接。

## 验证

- 只读对照：官方 `PROFILE_TEMPLATES`、CLI plugin/pnpm reconcile、Bundle 解析顺序、e2e 删除 `tui`、pager 默认 backend、TS 发布包依赖/`files`/`exports`、server version 与 backend stderr。
- 联网对照：DeepSeek Harness 官方 README、Bundle、package/install、Cordis lifecycle、CLI reference；npm 官方 package metadata、Trusted Publishing、staged publishing 与 publish integrity。
- `git diff --check`：通过。
- 未跑 `cargo test` / `pnpm run verify:ts`；本批次只变更 Markdown，没有改代码、manifest 或测试 fixture。

## Git 提交

- commit message：`docs: revise product plugin launcher architecture`
- Progress-Record trailer：`Progress-Record: docs/开发进度记录/2026-08-26_18-40-48_产品插件启动器方案.md`
- 暂存区审计：仅包含 `docs/PRODUCT_PLUGIN_LAUNCHER.md`、`docs/README.md` 和本进度记录，共 3 个文档文件。

## 阻塞与下一步

- 当前只交付设计；推荐 v2 的实现应按文档 PR 0–4 另立进度记录。
- 未解决：需用干净 npm 前缀验证私有 DSH/pnpm 启动链，并以实际 `npm pack --json` 产物设定体积门禁。
