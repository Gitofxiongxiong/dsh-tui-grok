# Real Flash request and complete E2E verification

> 记录时间：2026-08-23 11:06:16 +0800
> 操作者/agent：Codex `/root`
> 状态：completed

## Model

The `tui-embedded` profile resolves the default model to
`deepseek-official/deepseek-v4-flash`. Credentials remain in the Harness-owned
`$DSH_HOME` and are not copied into this repository.

## Real request

The one-shot request was:

```text
Reply with exactly DSH_FLASH_E2E_OK and nothing else.
```

`dsh --profile headless` returned exactly `DSH_FLASH_E2E_OK`.

The same prompt was then submitted through `target/debug/dsh-pager` over a PTY
against `/home/leo/code/deepseek-harness/apps/cli/lib/bin.js --profile
tui-embedded`. The response marker was observed, and the PTY process exited
successfully with alternate-screen and cursor restoration markers present.

## Test results

- `scripts/e2e.sh`: passed; protocol/source checks, 648-case M10 matrix,
  formatting, workspace check/tests, all-features clippy, binary build, and
  full mock PTY all passed.
- `REAL_E2E_SESSION=session-71569f6b-4d1f-4f4f-a13b-7f1613897a1b DSH_HARNESS_ROOT=/home/leo/code/deepseek-harness REAL_E2E_TIMEOUT=60 scripts/real-e2e.sh`:
  passed; real `tui.hello`, list, dashboard, 16,021-event load barrier, and
  isolated real PTY picker/resize/mouse/Esc/restore checks passed.
- Real model prompt/stream/complete/restore smoke: passed in an isolated new
  session; no existing session was mutated.

The real Harness checkout still contains its pre-existing uncommitted source
and test changes; they were not reverted or included here.
