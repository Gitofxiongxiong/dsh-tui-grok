# 同步 slash 来源清单哈希

> 记录时间：2026-08-30 09:55:09 +0800
> 操作者/agent：Codex
> 状态：completed

## 目标与背景

- 绝对路径 prompt 分类修复改变了已登记的 Grok B/A1 wrapper `src/slash.rs`。
- `scripts/e2e.sh` 的 source-manifest 门禁报告本地哈希漂移；本批只同步已审计的 vendored hash 和本地适配说明。

## 设计契约

- Grok upstream path、upstream SHA-256、mirror commit 和许可证保持不变。
- 复用等级仍为 B/A1；只补充“非合法 command token 的 leading-slash 文本走普通 prompt”这一 DSH 适配接缝。
- 不修改 Grok vendor 文件、许可证、NOTICE 或其他来源行。

## 计划修改范围

- `crates/dsh-pager-grok-ui/SOURCE_MANIFEST.md`
  - 更新 `src/slash.rs` 的 vendored SHA-256 和本地变更说明。
- `docs/开发进度记录/2026-08-30_09-55-09_同步slash来源清单哈希.md`
  - 记录门禁失败、同步结果、验证和提交审计。
- 不在范围内：`src/slash.rs` 本身、其他代码/文档/生成物。

## 风险、回滚和预期验证

- 风险：误改 upstream hash 会遮蔽真实来源漂移；仅以当前本地文件 `sha256sum` 更新 vendored 列。
- 回滚：revert 本记录对应的单独提交。
- 验证：`python3 scripts/check-source-manifest.py`、`scripts/e2e.sh`、`git diff --check`。

## 实际修改与验证

- 首次 `scripts/e2e.sh` 在功能修复后正确报告
  `local hash drift: src/slash.rs (28c618d9...)`，因此暂停其余门禁并按
  范围规则创建本记录。
- `SOURCE_MANIFEST.md` 的 manifest date 更新为 2026-08-30；仅将
  `src/slash.rs` vendored SHA-256 从
  `1ccf57f54e15b64a3e36add1a0bb532977f358f4ae36af7c9abfb88f2d835950`
  更新为
  `28c618d9a2e1724aeb45aec3ad927fd1f69f11a3be6b184bae825ad405869276`。
- 本地变更说明补充：command token 不满足共享 ASCII grammar（例如包含
  第二个 `/` 的绝对路径）时保持为普通 prompt；语法合法的拼错命令
  仍显式报错。upstream path/hash、mirror revision、license 均未改。
- `python3 scripts/check-source-manifest.py`：90 rows，local drift 0、
  upstream drift 0、missing license 0，通过。
- 再次 `scripts/e2e.sh`：通过 source manifest、protocol fixtures、972-case
  semantic parity、workspace fmt/check/test/clippy、build 和 full PTY smoke。
- `git diff --check`：通过。

## Git 提交

- 计划 commit message：`chore(source): sync slash adapter manifest`。
- Progress-Record trailer：`Progress-Record: docs/开发进度记录/2026-08-30_09-55-09_同步slash来源清单哈希.md`。
- 暂存区审计：本提交只包含 `SOURCE_MANIFEST.md` 和本记录；不夹带
  功能代码、README 或其他生成物。
