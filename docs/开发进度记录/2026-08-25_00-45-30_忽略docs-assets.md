# 忽略 docs/assets

> 记录时间：2026-08-25 00:45:30 +0800
> 操作者/agent：Grok
> 状态：completed

## 目标与背景

`docs/assets` 是 ttyd/xterm 浏览器截图目录，体积大且不应当入库。
AGENTS.md 也规定临时截图默认放在工作区外。把它加入 ignore，并把已经跟踪的
PNG 从 Git 索引移除，本地文件保留。

## 设计契约和复用依据

- 对应长期计划章节：验证门禁里的截图产物规则。
- 复用等级：不适用（仓库元数据）。
- 将替换的旧模块：无。

## 计划修改范围

- `.gitignore`：增加 `/docs/assets/`。
- 从 Git 索引取消跟踪 `docs/assets/**`（`git rm --cached`，不删工作区文件）。
- `docs/开发进度记录/2026-08-25_00-45-30_忽略docs-assets.md`：本记录。

不改功能点文档里的相对图片路径：本地仍可打开，只是不再随 commit 走。

## 风险、回滚与未决项

- 风险：历史 commit 里仍有这些 PNG；本次只停止后续跟踪。
- 回滚：从 `.gitignore` 去掉该行并重新 `git add docs/assets`。
- 未决项：不改历史 git。

## 实际修改

- `.gitignore` 增加 `/docs/assets/`。
- `git rm -r --cached docs/assets`：23 张 PNG 离开索引，工作区文件仍在。

## 实际验证

- `git check-ignore -v docs/assets/grok-tui-transcript/01-idle-welcome.png`：命中 `.gitignore:9:/docs/assets/`。
- `git ls-files docs/assets`：空。
- 磁盘上 `docs/assets/` 子目录仍在。
- `git diff --check`：提交前运行。

## 提交

- 计划提交信息：`chore: ignore docs/assets screenshots`
- trailer：`Progress-Record: docs/开发进度记录/2026-08-25_00-45-30_忽略docs-assets.md`
