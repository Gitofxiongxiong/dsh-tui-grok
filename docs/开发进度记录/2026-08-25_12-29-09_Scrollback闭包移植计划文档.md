# Scrollback 闭包移植计划文档

> 记录时间：2026-08-25 12:29:09 +0800
> 操作者/agent：Grok
> 状态：completed

## 目标与背景

运行态 rail 行波在 DSH 里被改成长度感知高斯彗星，出现整列变黑再从头来的
周期行为。根因不是刷新率，而是 `transcript.rs` 自造了一套 Grok 没有的视觉
算法。同一文件已超过 3900 行，把 chrome、行波、分组、thinking/tool 投影、
Markdown/Diff 和测试挤在一起；继续在这里打补丁会让像素级对齐越来越难。

本批只写档 3（完整 Grok scrollback 闭包替换 `RichTranscript`）的可执行计划，
拦住继续往该文件堆视觉实现，不改运行时代码。

## 设计契约和复用依据

- 对应长期计划章节：2.1 视觉契约、2.5 源码契约；
  `docs/GROK_RENDERER_PIXEL_PARITY_PLAN.md` N2。
- Grok source path、commit、SOURCE_REV：
  `xai-grok-pager/src/scrollback/` 整棵闭包（wrappers、blocks、render、layout、
  types、state/verb_group）以及 `xai-grok-pager-render` 的
  `theme/tokyonight.rs::wave_brightness`、`render/color.rs::blend_color`、
  `appearance/config.rs`、`glyphs.rs`；mirror commit
  `19d42e35c07a9c9244f03f6df0c4c353f970d4f9`，SOURCE_REV
  `7d67deacbeb1c1093fdb4f9bcbfab2630e18a6aa`。
- 复用等级：A0/A1 vendor 纯 renderer；B 适配 DSH identity/layout authority；
  C 仅 DSH 真源；D 排除 Grok agent/shell/ACP runtime。
- DSH-neutral seam：`dsh_pager::Scrollback` 继续拥有稳定 entry identity、
  Fenwick 高度和 anchor；Grok scrollback 只绘制。不新增协议 DTO。
- 稳定 ID/generation：不变。

## 计划修改范围

- 文件：
  - `docs/GROK_SCROLLBACK_CLOSURE_PLAN.md`（新建）
  - `docs/GROK_RENDERER_PIXEL_PARITY_PLAN.md`（N2 增加指向）
  - `docs/README.md`（索引）
  - 本进度记录
- 预期行为：给出冻结规则、Grok/DSH 目录对照、必须 vendor 的闭包、adapter
  接缝、按切片删除 `transcript.rs` 职责的顺序，以及若继续在该文件改行波会
  怎样加剧漂移。
- 不在范围内：任何 Rust/生产代码、vendor 拷贝、行波公式替换。

## 风险、回滚和依赖

- 风险：计划与已有 N2 记录、消息呈现计划重叠；本文明确自己是 N2 的执行细则，
  不取代总控计划。
- 回滚：删除新建文档并还原两处索引改动。
- 依赖：无代码依赖。工作区若有未提交的 `transcript.rs` 改动，本批不得暂存。

## 实际修改

- 文件：
  - `docs/GROK_SCROLLBACK_CLOSURE_PLAN.md`（新建）
  - `docs/GROK_RENDERER_PIXEL_PARITY_PLAN.md`
  - `docs/README.md`
  - 本进度记录
- 摘要：
  - 档 3 计划把转录生产路径定义为 vendor Grok `scrollback/` 闭包，DSH
    Fenwick 仍拥有 identity。
  - 立刻冻结在 `transcript.rs` 新增行波/chrome/block 算法；给出 S1–S7
    每刀必须删除的本地函数。
  - 上级 N2 与文档索引增加指向，避免第三份平行路线。

## 验证结果

- 只改文档，未跑 Rust 测试。
- `git diff --check`：待提交前执行。
- 未暂存工作区已有的 `transcript.rs` 未提交改动。

## Git 提交

- commit message：`docs: plan grok scrollback closure to stop transcript drift`
- Progress-Record trailer：
  `Progress-Record: docs/开发进度记录/2026-08-25_12-29-09_Scrollback闭包移植计划文档.md`
- 暂存区审计：仅上述 4 个文件。

## 未解决问题和下一步

代码尚未按 S1 替换行波。下一刀若动手，应直接 vendor/调用 Grok `sin²`，
不得再调高斯三常数。
