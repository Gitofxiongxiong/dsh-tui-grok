# Grok UI Clippy 门禁

> 记录时间：2026-08-29 08:19:34 -0400
> 操作者/agent：Codex
> 状态：partial（Grok UI 全绿；workspace 暴露最后一处 bin lint）

## 目标与背景

Rust 1.98 workspace all-features Clippy 在上一批核心 host 修复后继续报告 7 个 Grok UI lint：自有代码中的可派生 Default、可折叠条件、测试初始化、无效借用和高参数渲染 helper，以及未修改 vendored pager `model.rs` 中的相同分支。

目标是保持运行时行为、Grok source revision 和 vendored Rust source 不变，恢复 workspace Clippy，并继续执行完整 E2E 与 PTY 门禁。

## 设计契约和复用依据

- 对应长期计划：M0.5 Clippy、2.5 vendored source 契约、M9/M10 完整门禁。
- Grok mirror revision：`19d42e35c07a9c9244f03f6df0c4c353f970d4f9`。
- 自有 UI 代码使用 Rust 1.98 建议的等价写法；高参数 helper 保持现有调用契约并做定点 lint allow，避免为风格门禁引入跨函数重构。
- vendored `model.rs` 不直接修改；在本地 wrapper module 上做最窄范围的 lint allow，并同步 `SOURCE_MANIFEST.md` 的 wrapper hash/说明。

## 计划修改范围

- `docs/开发进度记录/2026-08-29_08-19-34_Grok-UI-Clippy门禁.md`
- `crates/dsh-pager-grok-ui/src/agent_preset.rs`
- `crates/dsh-pager-grok-ui/src/views/preset_picker.rs`
- `crates/dsh-pager-grok-ui/src/runtime.rs`
- `crates/dsh-pager-grok-ui/src/slash.rs`
- `crates/dsh-pager-grok-ui/SOURCE_MANIFEST.md`

不在范围内：不改 vendored `.rs`、protocol、业务行为、Grok revision 或真实 Harness/凭据。

## 风险、回滚和依赖

- 风险：派生 Default 必须与现有零值初始化一致；已核对 `PickerState`、`ModalWindowState` 与容器字段的 Default 语义。
- 风险：wrapper lint allow 必须只覆盖包含 vendored `model.rs` 的 module。
- 回滚：回退本记录对应提交；无数据迁移。
- 依赖：Rust 1.98.0、锁定 Grok checkout、Node/Python PTY 环境。

## 实际修改

- `src/agent_preset.rs`：折叠 roster/name 两层条件，保持 fallback 顺序。
- `src/views/preset_picker.rs`：使用等价的派生 `Default`。
- `src/runtime.rs`：定点允许现有高参数渲染 helper；测试用结构体初始化替代 Default 后字段重赋值；移除两个无效借用。
- `src/slash.rs`、`SOURCE_MANIFEST.md`：在 wrapper module 上定点允许未修改 vendored `model.rs` 的 `if_same_then_else`，同步 wrapper hash 与 A1 说明。

## 验证结果

- `cargo fmt --all -- --check`：通过。
- `python3 scripts/check-source-manifest.py`：通过，90 rows，local/upstream/license drift 均为 0。
- `cargo clippy -p dsh-pager-grok-ui --all-targets --all-features -- -D warnings`：通过。
- workspace all-features Clippy：本记录的 7 个 lint 全部消失；继续发现范围外 `dsh-pager-bin/src/main.rs` 1 个 `collapsible_if`，需独立记录处理。

## Git 提交

- commit message：`fix(build): clear Grok UI clippy gate`
- Progress-Record trailer：`Progress-Record: docs/开发进度记录/2026-08-29_08-19-34_Grok-UI-Clippy门禁.md`
- 暂存区审计：提交前以 `git diff --cached --name-only` 核对仅含本记录列出的 6 个文件。

## 未解决问题和下一步

- 独立修复 `dsh-pager-bin` 最后一处 Rust 1.98 lint，然后重跑 workspace Clippy、完整 E2E 和 full PTY。
