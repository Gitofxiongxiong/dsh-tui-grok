# 继续对齐 edition 2024 rustfmt 格式化

> 记录时间：2026-08-29 14:13:07 +0800
> 操作者/agent：assistant
> 状态：complete

## 目标与背景

工作树中有一批未提交改动，涉及 `dsh-pager` 的 `dashboard.rs`、`presentation.rs`、
`scrollback.rs`、`session.rs`、`transport.rs` 和 `dsh-pager-grok-ui` 的
`clipboard.rs`。逐行核对后这些改动只是 rustfmt 的 import 排序与 `assert!` 宏
参数重排，无任何功能逻辑变化。

项目既定格式化惯例是 `rustfmt --edition 2024`（见
`2026-08-29_13-44-29_扩展V4模型紧凑显示名.md` 与
`2026-08-29_12-27-49_prompt模式标签原子裁剪.md` 的验证记录）。HEAD 状态下
`cargo fmt --all -- --check`（默认 edition 2021）本就对
`dsh-pager-bin/src/main.rs`、`dsh-pager/tests/spawn_contract.rs`、
`dsh-pager/src/transport.rs`、`clipboard.rs` 报 diff，说明仓库实际以 edition 2024
风格格式化文件。本批继续把 `dsh-pager` 的剩余文件按同一风格对齐。

最近一次提交（`6d42d4a feat(tui): open model picker from model labels`）对应记录
明确排除这些文件：“不修改用户已有的 clipboard/dashboard/presentation/scrollback/
session/transport 未提交变更”，因此本批是独立格式化批次，需要单独记录与 commit。

## 设计契约和修改范围

- 纯格式化：只改变文件排版，不改变代码行为、协议、DTO 或测试语义。
- 计划文件（本批唯一记录）：
  - `docs/开发进度记录/2026-08-29_14-13-07_继续对齐edition2024格式化.md`
- 目标文件：
  - `crates/dsh-pager-grok-ui/src/clipboard.rs`
  - `crates/dsh-pager/src/dashboard.rs`
  - `crates/dsh-pager/src/presentation.rs`
  - `crates/dsh-pager/src/scrollback.rs`
  - `crates/dsh-pager/src/session.rs`
  - `crates/dsh-pager/src/transport.rs`
- 不触碰：vendor、协议 fixture、Cargo.lock、其他 crate、用户未在本批列出的
  其他未提交文件（本批已无其他未提交文件）。

## 风险、回滚和验证

- 风险：格式化不应改变语义；用 `rustfmt --edition 2024 --check` 全绿、
  `git diff -w` 复核以及 `git diff --check` 确认无语义漂移。
- 回滚：回滚本记录对应单一 commit；无数据迁移。
- 验证：
  - `rustfmt --edition 2024 --check`（本批 6 个文件）：通过；
  - `git diff --check`：通过；
  - `cargo check --workspace --locked`：通过；
  - `cargo test -p dsh-pager -p dsh-pager-grok-ui --locked`：通过。

## 实际修改

- `crates/dsh-pager-grok-ui/src/clipboard.rs`
  - 测试内 `capture_cli` 调用参数从多行改为单行数组字面量。
- `crates/dsh-pager/src/dashboard.rs`
  - 测试 `use serde_json::{json, Map, Value}` 的 import 排序重排。
- `crates/dsh-pager/src/presentation.rs`
  - 测试内 `assert!(...)` 宏单行重排为跨行。
- `crates/dsh-pager/src/scrollback.rs`
  - 测试 import 排序与 `assert!(...)` 宏重排。
- `crates/dsh-pager/src/session.rs`
  - 测试内两处 `assert!(...)` 宏重排。
- `crates/dsh-pager/src/transport.rs`
  - `dsh_pager_protocol`/`serde`/`serde_json` import 分组排序重排；
    `fail(...)` 单行字符串参数压缩。

## 验证结果

- `rustfmt --edition 2024 --check`（6 文件）：全部 CLEAN。
- `git diff -w`：复核仍显示的仅为 import 排序与宏参数重排，无语义改动。
- `cargo check --workspace --locked`：通过（exit 0）。
- `cargo test -p dsh-pager -p dsh-pager-grok-ui --locked`：通过。
  - `dsh-pager-grok-ui`：624 passed、0 failed；doctests 1 passed、1 ignored。
  - `dsh-pager`：测试通过（0 failed）。

## Git 提交

- commit message：`style(tui): align dsh-pager tests to edition 2024 rustfmt`。
- Progress-Record trailer：
  `Progress-Record: docs/开发进度记录/2026-08-29_14-13-07_继续对齐edition2024格式化.md`
- 暂存区审计：仅纳入本记录和上列 6 个目标文件；不包含无关格式化或生成物。

## 未解决问题和下一步

- 仓库仍未统一 crate 声明：`dsh-pager`、`dsh-pager-bin` 的 Cargo.toml 仍是
  edition 2021，而文件按 edition 2024 风格格式化，导致 `cargo fmt --all`
  （默认 edition）持续对部分文件报 diff。后续若决定整体升级到 edition 2024，
  应另开批次统一改动 workspace 各 crate 的 edition 声明与相应文件。
