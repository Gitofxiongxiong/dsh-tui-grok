# Real DeepSeek Harness configuration and E2E

> 记录时间：2026-08-23 10:49:38 +0800
> 操作者/agent：Codex `/root`
> 状态：completed

## 目标

使用 `/home/leo/code/deepseek-harness` 源码 checkout 作为真实 TUI backend，配置
可复现启动方式，并运行不调用模型、不修改既有 session 的 hello/list/dashboard/load
和 PTY 生命周期门禁。

## 配置

- backend：`/home/leo/code/deepseek-harness/apps/cli/lib/bin.js --profile tui-embedded`。
- `scripts/real-e2e.sh` 支持 `DSH_HARNESS_ROOT`、`DSH_TUI_SERVER`、
  `REAL_E2E_SESSION` 和 `REAL_E2E_TIMEOUT`；默认 load/PTY 使用隔离新 session，
  指定 `REAL_E2E_SESSION` 时 load 改为已有 session 的只读 attach。
- `scripts/pty-smoke.py` 新增 backend/pager 参数转发，保留原 mock 默认行为。
- 不把 API key 或 `$DSH_HOME` credentials 写入 successor 仓库。

## 实际验证

- Harness `pnpm run build:lib:host` 被其现有未提交测试错误阻断；随后执行
  `pnpm exec tsdown --env.DSH_BUILD_FACE host` 成功重建 source checkout 的 host
  bundles，修复了 stale `lib/index.js` 未识别 `tui.subscribe.scope=all` 的问题。
- 真实源码 backend 的 `tui.hello` 成功，server 为 `deepseek-harness-tui/0.1.0-rc.8`；
  `--list-sessions` 与 `--dashboard` 均成功。
- 显式 session `session-71569f6b-4d1f-4f4f-a13b-7f1613897a1b` 成功完成 load barrier，
  加载 16,021 events（seq `0..16020`）。真实 PTY 在隔离新 session 上通过 picker、
  resize/mouse、Esc ladder 和 terminal restore。
- 原默认最近 session 的真实 load 暴露 Harness 持久化日志损坏：line 192 期望 seq
  3084、实际 seq 2404；脚本不再自动选择该 session，保留该错误证据并要求显式
  `REAL_E2E_SESSION` 或使用隔离新 session。

## 未解决问题

- 本次未运行 `--smoke-interactions`、`--smoke-queue`、`--smoke-lifecycle`，因为它们
  会产生真实模型请求或修改/创建 session；需要用户明确指定测试 session、费用和副作用
  边界后再执行。
- Harness checkout 原有 22 个未提交源/文档改动未被本批次修改或提交。

## 提交

- `scripts/real-e2e.sh`、`scripts/pty-smoke.py`、`docs/TESTING.md` 和本记录在同一
  successor commit 中提交。
