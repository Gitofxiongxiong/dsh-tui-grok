# 协议/transport 测试改 Node mock，全平台运行

> 记录时间：2026-08-26 23:08:56 +0800
> 操作者/agent：Grok Build subagent
> 状态：completed

## 目标与背景

完成本地平台兼容 **step 4**：让协议/transport 测试在 Windows 上也能编译运行，而不只是 Unix。

已落地且本批不重开：

- Step 1 `6f013d0` — spawn 合同、拒绝 `.cmd`、stderr pipe
- Step 2 `e7f1ac6` — 启动脚本 `node + bin.js`
- Step 3 `ab5d373` — clipboard native+OSC52

当前 `transport_contract.rs`、`capability_effects.rs` 整文件 `#![cfg(unix)]`，只因为 mock 是 `sh` + `backend.sh`；`dsh-pager-test-support` 的 `process.rs` 单测用 `Command::new("sh")` `printf ok`。这些行为（畸形 JSON 噪声、乱序 RPC、file-reference/attachment 解析、timeout reaper）不是 Unix 特有系统调用。

## 设计契约和复用依据

- 对应长期计划章节：验证门禁 / 协议测试可重建；`docs/TESTING.md`；本地平台兼容 step 4。
- Grok source path、commit、SOURCE_REV：
  - 上游 `/home/leo/code/grok-build` mirror `19d42e35c07a9c9244f03f6df0c4c353f970d4f9`，SOURCE_REV `7d67deacbeb1c1093fdb4f9bcbfab2630e18a6aa`
  - Grok 需要子进程且关心 Windows 时用真实 binary，不用 `sh -c`
  - **明确不复制** Grok pager PTY harness `xai-grok-pager-pty-harness`（完整 ConPTY/PTY 产品）。`scripts/pty-smoke.py` 继续 Unix-only（`pty`/`termios`/`fcntl`），本步不改写成 Windows ConPTY
- 复用等级 A0/A1/B/C/D：
  - **B**：跟随本仓库已有便携 mock：`crates/dsh-pager/tests/spawn_contract.rs`（写 `.mjs` + `RpcTransport::spawn("node", &[script])`）和 `crates/dsh-pager-bin/tests/mock-server.mjs` + `hello.rs`（`--backend node --backend-arg <mjs>`）
  - **D**：若继续 `#!/bin/sh` 或 vendor Grok PTY harness — 禁止
- DSH-neutral seam：测试只 spawn Node stdio JSON-RPC mock；view 仍不调 RPC
- 新增 DTO/Intent/Effect：无
- 稳定 ID 和 generation：无
- 将替换的旧模块：测试内 `sh`/`backend.sh` mock，不是生产 UI
- 失败测试、PTY/golden 和回滚：`cargo test -p dsh-pager/bin/test-support`；无用户可见 TUI 变化，不跑浏览器像素对比。回滚还原本记录文件列表。

## 计划修改范围

- 文件：
  - `docs/开发进度记录/2026-08-26_23-08-56_协议transport测试改Node跨平台.md`
  - `crates/dsh-pager-test-support/src/node_mock.rs`（新）：写临时 `.mjs`、校验 `node` 在 PATH、UTF-8 argv helper
  - `crates/dsh-pager-test-support/src/lib.rs`：导出 helper
  - `crates/dsh-pager-test-support/src/process.rs`：timeout 单测改为 `node -e process.stdout.write('ok')`，去掉 `#[cfg(unix)]`
  - `crates/dsh-pager/tests/transport_contract.rs`：去掉 `#![cfg(unix)]`，Node mock 保留两个测试
  - `crates/dsh-pager/tests/capability_effects.rs`：同上
  - `crates/dsh-pager-bin/tests/hello.rs`：`to_str().expect("utf-8 path")` 改为带上下文的 UTF-8 转换（Cargo target dir 通常是 UTF-8）
  - `docs/TESTING.md`：一句说明协议测试用 Node mock 跨平台，PTY smoke 仍 Unix
- 预期行为：
  1. `transport_contract` 两个测试在非 Unix 上也编译运行：畸形 JSON 忽略 + 通知缓存；乱序 completion 不丢 notification。
  2. `capability_effects` 两个测试同样：file-reference path/kind；attachment metadata+data。
  3. Spawn：`RpcTransport::spawn("node", &[script])`，与 `spawn_contract.rs` 一致。缺 Node 时失败信息明确。
  4. JSON-RPC 行用 `\n`；reader 已 strip `\r`。
  5. `process.rs` 单测全平台跑；不为 timeout reaper 保留 `cfg(unix)`。
  6. 测试源码不再出现 `/bin/sh`、`#!/bin/sh`、`Command::new("sh")`，除非真正 Unix syscall（本批不应有）。
- 不在范围内：
  - clipboard、spawn 合同、launch scripts（step 1–3）
  - `scripts/pty-smoke.py` 改 Windows ConPTY
  - `.github/workflows`、npm native packages、Trusted Publishing（step 5）
  - musl、PowerShell
  - 未跟踪文件 `docs/开发进度记录/2026-08-25_21-53-51_计划审批问答提交被拒.md`
  - 不改 `spawn_contract.rs`（step 1 已用 Node）

## 风险、回滚和依赖

- 本机 Linux 不能跑 MSVC；Windows 编译靠去掉 `cfg(unix)`/`sh` 来保证，而不是在本机交叉测。
- Node 必须在 PATH；缺失时应明确失败，不能静默 skip。
- Node stdout 在非 TTY 上可能块缓冲；小 `process.stdout.write` 与现有 `mock-server.mjs`/`spawn_contract.rs` 一致，已验证可用。
- 回滚：还原本记录列出的文件。

## 实际修改

- 文件：
  - `docs/开发进度记录/2026-08-26_23-08-56_协议transport测试改Node跨平台.md`
  - `crates/dsh-pager-test-support/src/node_mock.rs`（新）
  - `crates/dsh-pager-test-support/src/lib.rs`
  - `crates/dsh-pager-test-support/src/process.rs`
  - `crates/dsh-pager/tests/transport_contract.rs`
  - `crates/dsh-pager/tests/capability_effects.rs`
  - `crates/dsh-pager-bin/tests/hello.rs`
  - `docs/TESTING.md`
- 摘要：
  - 抽出 `NodeStdioMock`：写 hermetic `backend.mjs`、`require_node()` 缺 Node 时明确失败、`utf8_path_arg`、`echo_line` 对每条 stdin 回同一 JSON-RPC 行（`process.stdout.write` + `\n`）。
  - `transport_contract` / `capability_effects` 去掉 `#![cfg(unix)]` 与 `sh`/`backend.sh`；spawn `node` + `.mjs`，两个测试各自保留。
  - `process.rs` timeout 单测改为 `node -e process.stdout.write('ok')`，全平台运行。
  - `hello.rs` 用 `utf8_path_arg` 替代 `to_str().expect("utf-8 path")`。
  - 未改 `spawn_contract.rs`、`pty-smoke.py`、clipboard、launch scripts、GitHub Actions。
  - 未 vendor Grok `xai-grok-pager-pty-harness`。

## 验证结果

- 命令：
  - `cargo fmt -p dsh-pager -p dsh-pager-bin -p dsh-pager-test-support`
  - `cargo test -p dsh-pager-test-support --locked`
  - `cargo test -p dsh-pager --locked`
  - `cargo test -p dsh-pager-bin --locked`
  - `cargo check --workspace --locked`
- 结果：
  - test-support：9 passed（含 `writes_mjs_under_sandbox` 与 `bounded_runner_reaps_successful_child_and_captures_output`）
  - dsh-pager：89 lib + 2 capability_effects + 2 transport_contract + 2 spawn_contract + 其余 fixture 测试 passed
  - dsh-pager-bin：5 argv 单测 + 14 hello.rs passed
  - `cargo check --workspace --locked` 通过
  - 本机 Linux，未跑 MSVC。测试源码不再使用 `/bin/sh` / `#!/bin/sh` / `Command::new("sh")`。
  - 无用户可见 TUI 布局变化，未跑浏览器像素对比。

## Git 提交

- commit message：`test: run pager protocol mocks with node on all OSes`
- Progress-Record trailer：`docs/开发进度记录/2026-08-26_23-08-56_协议transport测试改Node跨平台.md`
- 暂存区审计：仅上述列出文件；不含 `docs/开发进度记录/2026-08-25_21-53-51_计划审批问答提交被拒.md`。

## 未解决问题和下一步

- Step 5：npm native packages / GitHub Actions release matrix。
- `scripts/pty-smoke.py` 仍 Unix-only（本步明确不改）。
- `clipboard.rs` 仍有 `#[cfg(unix)]`：Linux/WSL CLI tool 单测（step 3），不是协议 mock。
- 本机不能证明 MSVC 编译；Windows 依赖去掉 `cfg(unix)`/`sh` 后的源码可移植性。
