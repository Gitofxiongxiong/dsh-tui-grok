# 补齐运行态 Rail 与思考耗时

- 时间：2026-08-24 23:46（Asia/Shanghai）
- 状态：已完成
- 目标：对齐 `docs/开发进度记录/2026-08-24_22-06-47_Grok转录区块TUI功能点.md` 与 `/home/leo/code/grok-build` 的转录表现，补齐运行中的思考/工具 Rail 波形、完成态思考耗时文案及必要的帧刷新。
- 修改范围：
  - `crates/dsh-pager-grok-ui/src/views/transcript.rs`
  - `crates/dsh-pager-grok-ui/src/runtime.rs`
  - `crates/dsh-pager-grok-ui/src/host_adapter.rs`
  - `crates/dsh-pager-grok-ui/src/parity.rs`
  - `crates/dsh-pager-grok-ui/src/views/interaction.rs`（测试 fixture 字段补齐）
  - `crates/dsh-pager-grok-ui/src/views/agent_panes.rs`（仅修复本轮完整 clippy 门禁的测试 fixture lint）
  - `crates/dsh-pager/src/presentation.rs`
  - `crates/dsh-pager/src/scrollback.rs`
  - 对应 Rust/脚本测试文件
  - 本进度记录及必要的测试文档
- 验证范围：Rust 单测、工作区测试/格式检查、已有 E2E，以及 ttyd + xterm.js + Playwright 浏览器验证；截图写入 `/tmp`，不纳入仓库。

## 进度

- [x] 阅读功能点文档和 Grok Build 对应实现。
- [x] 核对 DSH 当前转录数据模型、缓存和帧刷新路径：为稳定流式 surface 补齐开始/结束 epoch 毫秒，并贯通 presentation → scrollback → Grok host adapter。
- [x] 实现运行态 Rail 波形、完成态闪烁和 `Thought for …`：按 Grok Build 的 `WAVE_SPEED=0.15`/32 行相位重绘，运行时 33ms 帧刷新；思考完成后显示 `Thought for 10.0s`，最近完成的 thinking/tool 在 400ms 内保留亮色 flash。
- [x] 完成自动化与浏览器验证并修复发现的问题：PTY smoke 增加 ANSI 增量绘制的屏幕重建，避免动态帧导致 `/resume` 标题误报；浏览器验证覆盖 idle、thinking 两个时间点、tool running、completed。
- [x] 更新本记录为完成并交付结果。

## 实现要点

- `ScrollbackPane` 保存帧 tick，运行中的 thinking/tool rail 使用 traveling wave；运行中的 thinking 同时渲染 `◆ Thinking…` 与思考正文。
- thinking surface 的开始/完成时间由 presentation 层记录并保留到最终 entry；折叠态按 Grok 规则显示 `◆ Thought for 10.0s`（超过一分钟使用 `XmYs`）。
- 最近完成的 thinking/tool rail 使用 400ms finish flash，runtime 在 running rail 或 flash 尚未结束时以约 33ms 轮询，结束后自动回到静态 rail。
- `scripts/pty-smoke.py` 现在维护最小 ANSI 屏幕模型，能够验证 ratatui 的增量 cell paint，不再把去控制符后的字节流误当成完整屏幕文本。

## 验证记录

- `cargo test --workspace --locked`：通过（含 `dsh-pager-grok-ui` 367 tests）。
- `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings`：通过。
- `cargo fmt --all -- --check`、`git diff --check`：通过。
- `bash scripts/e2e.sh`：通过（M8–M10、PTY、workspace tests）。
- `REAL_E2E_TIMEOUT=60 DSH_TUI_INSTALL_LOCAL=1 ./scripts/real-e2e.sh`：通过（真实 DeepSeek Harness hello/list/dashboard/load/PTY）。
- `python3 scripts/pty-smoke.py --binary target/debug/dsh-pager --timeout 30`：通过。
- `ttyd + xterm.js + Playwright`：无 console/page error；截图位于 `/tmp/dsh-pager-grok-browser-20260825/`：
  - `02-thinking-a.png` / `03-thinking-b.png`：运行态 Rail 与底部 thinking elapsed 随时间变化。
  - `04-tool-running.png`：`◆ Thought for 3.0s` 与运行中 `› Run sleep 4`。
  - `05-complete.png`：完成后的工具/思考 rail 与最终消息。
