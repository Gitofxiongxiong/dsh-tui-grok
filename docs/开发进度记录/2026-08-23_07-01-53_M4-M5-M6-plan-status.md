# M4/M5/M6 vertical slice 主计划状态回写

> 记录时间：2026-08-23 07:01:53 +0800
> 操作者/agent：Codex
> 状态：completed

## 目标与背景

将 2026-08-23 的 M4/M5/M6 Grok vertical slice 代码证据回写到长期主计划，
明确已进入默认生产路径的范围，以及仍需 reference/golden、selection 和
workspace hierarchy 才能宣称完整里程碑完成的范围。

## 设计契约和复用依据

- 对应长期计划章节：6.1、13、M4.1/M4.3/M4.4、M5.1、M6.3/M6.4。
- Grok source path、commit、SOURCE_REV：`vendor/grok`，mirror
  `19d42e35c07a9c9244f03f6dfc0c4c353f970d4f9`，`7d67deacbeb1c1093fdb4f9bcbfab2630e18a6aa`。
- 复用等级：A1/B；无 Grok agent/runtime 引入。

## 计划修改范围

- `docs/GROK_BUILD_TUI_DEEPSEEK_ADAPTATION_PLAN.md`
- 本记录文件

仅修改 M4/M5/M6 状态表、立即执行顺序和垂直切片证据；不修改代码、协议、
vendor 或测试 fixture。

## 风险、回滚和依赖

状态回写不能把迁移基线表述成完整 Grok parity。回滚只恢复本记录和计划文档；
代码批次及其独立进度记录不受影响。

## 实际修改

主计划 6.1 已将 M4/M5/M6 更新为“vertical slice/attach 基线已完成”，并逐项
记录结构化 block + scrollback window、multiline prompt/draft receipt、stable
ID picker + attach/load barrier 的证据和下一门禁。第 13 节已标注对应执行项已
完成迁移基线但保留 reference/性能、selection、dashboard/workspace 后续出口。

## 验证结果

`git diff --check`：通过。

## Git 提交

本批只包含主计划和本记录，使用本记录路径作为 `Progress-Record` trailer。

## 未解决问题和下一步

后续继续 M4.5-M4.8、M5.6-M5.8、M6.6-M6.8；不得把当前状态表解释为最终
M10 Grok parity/release 完成。
