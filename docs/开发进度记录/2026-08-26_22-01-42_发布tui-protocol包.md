# 发布第一个 npm 包 @dsh-pager-grok/tui-protocol

> 记录时间：2026-08-26 22:01:42 +0800
> 操作者/agent：main agent
> 状态：completed

## 目标与背景

用户已用账号 `leo-dai` 登录 npm，并拥有组织 `@dsh-pager-grok`。按依赖 DAG，
第一个可发布包是 `@dsh-pager-grok/tui-protocol`：它没有 `workspace:*`，纯库。
当前 `files` 只包含 `lib/index.js`、`lib/invariant.js` 和 `lib/**/*.d.ts`，
会漏掉运行时真正 import 的 `codec.js` / `constants.js` / `ids.js` / `methods.js` /
`types.js`。本批次修正 `files`，打包审计后向 npm 公开发布 `0.1.0`。

## 设计契约和复用依据

- 对应计划：`docs/PRODUCT_PLUGIN_LAUNCHER.md` 发布契约；packed tarball 必须含
  `lib/**/*.js` 与 `lib/**/*.d.ts`，排除 `*.tsbuildinfo`；无 lifecycle compile。
- Grok source path、commit、SOURCE_REV：不适用（不复制 Grok 源码）。
- 复用等级：不适用。

## 计划修改范围

- 文件：
  - `packages/dsh-tui-protocol/package.json`（扩 `files`，补 `repository.url`）
  - 本进度记录
- 预期行为：`npm pack` 产物含全部 protocol JS 入口；`npm publish` 得到
  `@dsh-pager-grok/tui-protocol@0.1.0`。
- 不在范围内：不发布 server / recovery / embedded / cli；不改协议、Rust、vendor。

## 风险、回滚和依赖

- 风险：tarball 仍缺文件 → pack 后解包审计 `lib/*.js`。
- 风险：scope 无权限 → 已确认 `leo-dai` 为 `dsh-pager-grok` owner。
- 回滚：`npm unpublish @dsh-pager-grok/tui-protocol@0.1.0`（72 小时内）；git 还原
  `package.json`。
- 依赖：本机 npm 登录；registry 可写。

## 实际文件与摘要

- `packages/dsh-tui-protocol/package.json`：`files` 改为 `lib/**/*.js` + `lib/**/*.d.ts`；补 `repository.url`。
- 已向 npm 公开发布 `@dsh-pager-grok/tui-protocol@0.1.0`（tag `latest`）。
- tarball 18 个文件，含 codec/constants/ids/index/invariant/methods/types 的 js+d.ts；无 tsbuildinfo、无 source map、无 `workspace:*`。

## 验证

- `pnpm --filter @dsh-pager-grok/tui-protocol test`：17 passed。
- `npm pack` 文件清单与发布 tarball 一致，shasum `f6e530f11aa5a67d7601bd6ca509242269a9c80f`。
- `npm publish --access public`：PUT 200，`+ @dsh-pager-grok/tui-protocol@0.1.0`。
- 发布后 `npm access get status`：`public`。
- registry packument：`dist-tags.latest = 0.1.0`，tarball
  `https://registry.npmjs.org/@dsh-pager-grok/tui-protocol/-/tui-protocol-0.1.0.tgz`。
- 干净 cache 的 `npm view`：name/version/`latest` 均为 `0.1.0`。

## Git 提交

- commit message：`build: publish @dsh-pager-grok/tui-protocol@0.1.0`
- Progress-Record trailer：`Progress-Record: docs/开发进度记录/2026-08-26_22-01-42_发布tui-protocol包.md`
- 暂存区仅包含上述 `package.json` 与本进度记录。

## 阻塞与下一步

- 未发布 server / recovery / embedded；它们仍含 `workspace:*` 或过窄 `files`。
- v2 的 runtime Bundle / CLI / 平台包仍未实现。
- 本次发布消耗了一枚 npm 2FA 恢复码；建议在 npm 账户设置里重新生成恢复码。

