# 完整测试与浏览器验证

状态：已完成

## 目标

针对冷会话投影恢复插件完成完整验证，补足之前遗漏的真实浏览器终端链路；如果
浏览器、PTY、真实 Harness 或完整门禁暴露问题，直接修复并复验。

## 背景

前一轮已经完成 TypeScript、Rust、JSON-RPC、PTY 和真实 profile 验证，但没有使用
`ttyd + xterm.js + Playwright` 检查最终浏览器渲染链路。仓库规则要求用户可见 TUI
变更必须完成浏览器截图验证；本轮需要明确该插件相关 UI 是否保持可用，并记录所有
差异和环境条件。

## 计划文件与产物

- 本记录文件；
- `docs/TESTING.md`、`docs/EXTERNAL_DSH_PLUGIN.md`（修正四包本地 profile 流程文档）；
- 如测试发现缺陷，允许修改对应的插件、profile 接线、Rust UI/runtime、测试和文档文件；
- 临时浏览器截图、diff 和日志放在 `/tmp/dsh-pager-grok-browser-*`，不提交仓库。

## 验证契约

- TypeScript package build/typecheck/test、Rust fmt/check/test/clippy、协议/source
  manifest、完整 `scripts/e2e.sh` 必须通过；
- 真实 DSH profile 必须验证冷会话 `session.list` 投影恢复；
- DSH TUI 和 `/home/leo/.grok/bin/grok` 分别由 ttyd 承载，通过真实浏览器 Playwright
  keyboard/mouse 输入驱动，固定 viewport、DPR、字体、字号、主题、终端尺寸和 locale；
- 截取 `.xterm` 最终像素，等待稳定帧；差异必须分类为预期、数据、环境或缺陷，未解释
  的差异不能直接宣称完成。

## 风险与回滚

- Grok 基准可能依赖本机登录状态或旧 profile；不得把真实凭据、私有会话或截图写入仓库；
- 浏览器验证若只因工具、字体或基准不可用失败，记录为阻塞，不伪造通过；
- 代码修复可按本记录列出的文件逐项回退，不使用 broad reset。

## 预期验证

- `pnpm install --frozen-lockfile`、`pnpm run verify:ts`；
- `bash scripts/e2e.sh`、真实 profile `scripts/real-e2e.sh`；
- `ttyd + Playwright` DSH/Grok 双端截图和差异审计；
- `git diff --check`，并确认 `/home/leo/code/deepseek-harness` 保持 clean。

## 实际结果

- `pnpm install --frozen-lockfile`、`pnpm run verify:ts`：通过；四个 TypeScript 包均
  build/typecheck，protocol 16、server 37、embedded 2、recovery 6 个测试通过。
- `bash scripts/e2e.sh`：通过；协议 fixture、source manifest、972-case semantic
  parity、workspace test、clippy、binary build 和 full PTY smoke 全部通过。
- 浏览器环境：`ttyd 1.7.7-40e79c7`、Playwright `1.62.1`、Chromium headless，viewport
  `1200x800`、DPR 1、`DejaVu Sans Mono` 16px、dark theme、`xterm-256color`、`en-US`。
- DSH 通过真实浏览器打开 ttyd 页面，等待稳定帧后截取 `.xterm`：空会话、`/resume`
  列表、`1200x800 → 900x600` resize、Escape 关闭，以及 `/timestamps off`、`on`、toggle
  输入均无 console/page error；`/resume` 的 screen-reader 文本确认既有会话标题可见。
- Grok 基准通过同样的 ttyd/Playwright 链路打开并截图；其无当前工作目录会话时，
  Resume overlay 显示 `No sessions found for this directory`，属于基准数据差异。
- 临时产物全部在 `/tmp/dsh-pager-grok-browser-20260824/`，没有写入仓库。DSH 与 Grok
  产品首页和 Resume chrome 的布局角色可见；截图中的中文方框是测试机没有 CJK 字体、
  而不是 UI 字符串或布局缺陷，归类为环境差异，不伪造 pixel parity。
- 第一次浏览器启动命令把 `--backend-arg=--profile` 误作为一个参数，导致 DSH 页面显示
  `unknown argument`；改为 CLI 正确的两个参数 `--backend-arg --profile` 后通过。这是
  测试命令错误，不是仓库代码问题。
- 完整测试还发现 `docs/TESTING.md` 和外置插件开发期说明仍写“三包”；已同步改为
  protocol/server/embedded/session-projection-recovery 四包流程。
- `git diff --check`：通过；`/home/leo/code/deepseek-harness`：clean。

## 未解决问题

本轮没有发现需要修改插件或 Rust 运行时代码的功能缺陷。由于本仓库是 native TUI、
不是浏览器应用，浏览器验证的目标是 ttyd/xterm.js 最终渲染和真实输入链路；DSH/Grok
不同产品首页不作为逐像素相等结论。测试机缺少 CJK 字体，中文 glyph 差异已单独记录。
