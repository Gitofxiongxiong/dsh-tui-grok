# Harness 0.1.2-alpha.1 完整验证收口

> 记录时间：2026-08-29 23:44:46 +0800
> 操作者/agent：Codex
> 状态：completed

## 任务范围

- 在 `upgrade/harness-0.1.2-alpha.1` 分支上独立复核 Harness `dsh-v0.1.2-alpha.1` 升级后的全部自动化测试。
- 使用隔离的 `/home/leo/code/deepseek-harness-latest` 和临时 `DSH_HOME` 运行真实 Harness 会话，不修改原始 `/home/leo/code/deepseek-harness` 和用户配置。
- 按固定 ttyd/xterm.js/Playwright 参数完成 DSH 与 Grok 的视觉、键盘、鼠标、粘贴和 resize 验证。
- 本次不修改产品代码、fixture、vendor 源码或既有历史记录；本文件是唯一 tracked 变更。

## 设计契约和计划修改范围

- 对应长期计划章节：质量门禁、真实 Harness E2E、Grok 视觉同源与浏览器终端截图对比。
- Grok source path / commit / SOURCE_REV：本次不修改或复制 Grok 源码；基准程序为 `/home/leo/.grok/bin/grok` 1.0.13（`5e9a58528b76`）。
- 复用等级：不适用；本批次是验证收口，没有 UI 实现变更。
- 计划及实际修改文件：仅本进度记录。
- 预期行为：全部自动化门禁和固定环境视觉验证通过，或明确记录真实阻塞/缺陷。
- 不在范围内：产品实现、fixture、vendor、source manifest、既有进度记录、原始 Harness checkout 和用户配置。

## 开始前状态与约束

- 基线提交：`2dafada feat: 发布 Harness 0.1.2-alpha.1 升级快照`。
- 工作树在开始验证和创建本记录前均为 clean。
- 既有升级记录保留当时“视觉验证受外部环境阻塞”的事实；本记录单独记录后续环境准备完成后的验证收口，不回写历史结论。
- 真实 Harness 验证使用临时 profile `dsh-pager-grok-alpha-validation`，没有发送模型请求，也没有使用用户凭据。
- 回滚方式：仅需 revert 本记录对应提交；不存在产品代码回滚项。

## 自动化测试结果

### TypeScript / Node

- 在独立 Harness checkout 中执行 `pnpm install --frozen-lockfile`、`pnpm run build:lib:host`、`pnpm run build:lib:client`，均通过。
- 在本项目执行 `pnpm install --frozen-lockfile` 和 `pnpm run verify:ts`，通过。
- TypeScript/Node 测试共 67 项通过：protocol 16、server 36、embedded 3、runtime 1、CLI 11。
- runtime/CLI pack verification、native package matrix（5 个包）及类型检查均通过。

### Rust / fixture / PTY

- `scripts/e2e.sh`：通过。
  - protocol fixture 2 份同步；source manifest 90 行，local/upstream drift 0，license 缺失 0。
  - semantic parity：972 cases、8 fixtures 全部通过。
  - workspace fmt、check、test、all-targets/all-features clippy `-D warnings`、build 和 mock PTY 全部通过。
- `cargo check --workspace --all-targets --locked`：通过。
- `cargo test --workspace --all-targets --locked`：通过，0 failure。
- `python3 scripts/pty-esc-parity.py --binary target/debug/dsh-pager`：通过。
- `python3 scripts/pty-stream-scroll.py --binary target/debug/dsh-pager`：通过；`marker=235 row=14->15, down_reports=24, tail=31->67`。

### 真实 Harness E2E

- 使用 `/home/leo/code/deepseek-harness-latest`（tag `dsh-v0.1.2-alpha.1`，commit `cd5ef81481`）与临时 `DSH_HOME=/tmp/dsh-pager-alpha-e2e.vRq6fF`。
- `scripts/real-e2e.sh` 的 hello、list、dashboard、load、PTY 全部通过。
- 创建并验证隔离 session `session-2465afe0-edf1-44a6-825a-72a4b89569e3`；未发送模型 prompt。
- 首次运行发现 Harness client-side libraries 尚未 build；执行 Harness 自身 `pnpm run build:lib:client` 后复跑通过，归类为独立 checkout 的构建准备问题，不是本项目回归。
- 通过命令：

  ```bash
  DSH_HOME=/tmp/dsh-pager-alpha-e2e.vRq6fF \
    DSH_HARNESS_ROOT=/home/leo/code/deepseek-harness-latest \
    DSH_TUI_PROFILE=dsh-pager-grok-alpha-validation \
    DSH_TUI_INSTALL_LOCAL=1 scripts/real-e2e.sh
  ```

## 视觉验证

### 固定环境

- ttyd `1.7.7`、xterm.js 页面、Playwright `1.61.1`、Chromium headless shell 1234。
- viewport `1200x800`、device scale factor `1`、`DejaVu Sans Mono 16`。
- 主题：background `#0a0a0a`、foreground `#e1e1e1`。
- DSH 使用显式 mock history session `session-mock`；Grok 使用隔离空目录和 `--no-plan --no-subagents --disable-web-search`，未触发模型请求。
- DSH 启动命令和状态：

  ```bash
  env -u NO_COLOR TERM=xterm-256color COLORTERM=truecolor \
    /home/leo/.local/bin/ttyd -i 127.0.0.1 -p 7682 -W -T xterm-256color \
    -t fontSize=16 -t 'fontFamily=DejaVu Sans Mono' \
    -t screenReaderMode=true \
    -t 'theme={"background":"#0a0a0a","foreground":"#e1e1e1"}' \
    target/debug/dsh-pager --session session-mock --backend node \
    --backend-arg crates/dsh-pager-bin/tests/mock-server.mjs
  ```

  启动成功，历史、tasks、queue 和 subagent fixture 可见。

- Grok 启动命令和状态：

  ```bash
  env -u NO_COLOR TERM=xterm-256color COLORTERM=truecolor \
    /home/leo/.local/bin/ttyd -i 127.0.0.1 -p 7683 -W -T xterm-256color \
    -t fontSize=16 -t 'fontFamily=DejaVu Sans Mono' \
    -t screenReaderMode=true \
    -t 'theme={"background":"#0a0a0a","foreground":"#e1e1e1"}' \
    /home/leo/.grok/bin/grok --fullscreen \
    --cwd /tmp/dsh-grok-visual-cwd.vU6KXg \
    --no-plan --no-subagents --disable-web-search
  ```

  启动成功，隔离空会话 prompt 可见。

### ESC 对齐

- `scripts/playwright-esc-parity.mjs` 通过。
- 输入序列：分别输入 `xraft-browser-clear`，第一次 ESC 验证 clear hint；DSH 清空后第二次 ESC 打开 rewind picker，再以 Enter 打开 confirm；Grok 空历史下验证第二次 ESC 不错误打开 rewind。
- DSH：clear hint、rewind picker、rewind confirm 全部出现，browser errors 为空。
- Grok：clear hint 出现；空历史第二次 ESC 被吞掉且未错误打开 rewind，browser errors 为空。
- 证据目录：`/tmp/dsh-esc-playwright-20260829-alpha-validation`。
- 执行命令：

  ```bash
  PLAYWRIGHT_MODULE=/home/leo/code/deepseek-harness-latest/node_modules/.pnpm/playwright@1.61.1/node_modules/playwright \
    PLAYWRIGHT_CHROMIUM=/home/leo/.cache/ms-playwright/chromium_headless_shell-1234/chrome-headless-shell-linux64/chrome-headless-shell \
    node scripts/playwright-esc-parity.mjs \
    --dsh-url http://127.0.0.1:7682 \
    --grok-url http://127.0.0.1:7683 \
    --output /tmp/dsh-esc-playwright-20260829-alpha-validation
  ```

### 鼠标、粘贴与 resize

- 浏览器鼠标点击后，DSH/Grok 的 `.xterm-helper-textarea` 均获得焦点。
- 在聚焦 textarea 上派发带 `text/plain` DataTransfer 的 `ClipboardEvent`；两端 marker 均进入输入框。原生 `Ctrl+V` 另行探测时，headless Chromium 不会合成操作系统 paste 事件，因此使用浏览器 DOM paste 事件覆盖 xterm.js 粘贴路径。
- viewport 从 `1200x800` 调整为 `900x600` 后，两端 terminal 均从 `130x41` 调整为 `97x31`，marker 保留，双 ESC 可清空，browser errors 为空。
- 截图均直接捕获 `.xterm` 元素；证据目录：`/tmp/dsh-browser-input-validation`。
- 稳定帧检查：语义状态到位后隐藏 xterm cursor layer 并把浏览器 context 设为 offline，以冻结 WebSocket 驱动的任务计时刷新；DSH/Grok 两张间隔 150 ms 的 `.xterm` PNG 均在首次比较时 SHA-256 完全一致，结果见同目录 `stability.json`。

### 并排人工截图审查与差异分类

- DSH clear、rewind picker、rewind confirm、粘贴和 resize 帧均无裁切、覆盖、边框断裂或异常重绘。
- Grok clear、idle、粘贴和 resize 帧均无裁切、覆盖、边框断裂或异常重绘。
- DSH 的 history/tasks/queue/subagent 内容与 Grok 空会话不同，属于 fixture/product data 差异，不是像素回归。
- DSH 的中文模式标签显示为 tofu 方框，原因是固定验证字体 `DejaVu Sans Mono` 缺少 CJK glyph；布局和边界正常，归类为测试环境字体差异。
- clear hint 文本、输入框形态、底部交互反馈与 Grok 基线一致；未发现需要修改产品代码的视觉缺陷。

## Git 提交

- 计划 commit message：`test: close alpha harness validation gates`。
- Progress-Record trailer：`Progress-Record: docs/开发进度记录/2026-08-29_23-44-46_Harness_0.1.2_alpha.1完整验证收口.md`。
- 暂存区审计：提交前确认仅包含本记录，`git diff --cached --check` 通过。

## 未解决问题和下一步

- 没有阻塞升级交付的产品问题。
- 测试环境若需正常显示中文，应安装包含 CJK glyph 的 fallback 字体；这不影响当前布局和交互验收。

## 最终结论

- Harness `0.1.2-alpha.1` 升级实现通过 TypeScript、Rust、fixture、PTY、真实 Harness E2E 和浏览器视觉验证。
- 没有发现产品代码回归，不需要代码修复。
- 本轮验证关闭既有升级记录中尚未完成的视觉验证项；证据保存在上述 `/tmp` 目录，未加入仓库。
