# 升级 Harness 范围扩展：PTC 预设与双语校验

## 时间

- 开始：2026-08-29 22:08:00 +0800
- 结束：2026-08-29 22:37:29 +0800
- 状态：完成

## 背景

实施 `0.1.2-alpha.1` 升级时确认新版 Harness 将内置 Code Mode preset ID 从
`code` 改为 `ptc`。原升级记录覆盖了 profile，却没有覆盖 Grok UI 的内置标签和
picker 回归；若不调整，用户会看到旧 ID 并向 Harness 提交无效选择。与此同时，
原记录内的 embedded 双语 README 已同步修改，需要更新配对哈希元数据。

## 修改范围

- `crates/dsh-pager-grok-ui/src/agent_preset.rs`
- `crates/dsh-pager-grok-ui/src/views/preset_picker.rs`
- `packages/dsh-tui-embedded/README.i18n.yaml`
- `docs/开发进度记录/2026-08-29_22-08-00_升级Harness范围扩展-PTC预设与双语校验.md`

## 验证

- `cargo test -p dsh-pager-grok-ui agent_preset`
- `cargo test -p dsh-pager-grok-ui preset_picker`
- `pnpm run verify-translation-pairing`
- 纳入主升级记录的完整 Rust、TypeScript、PTY 与浏览器视觉门禁。

## 回滚

以独立提交落地，使用普通 `git revert` 回滚；不得改写主升级提交或此前历史。

## 结果

- 内置 preset 显示与 picker 已从旧 `code` ID 切换为 0.1.2-alpha.1 的 `ptc`，
  未知旧 ID 继续按未知值处理，避免静默提交无效 preset。
- `cargo test -p dsh-pager-grok-ui agent_preset`、
  `cargo test -p dsh-pager-grok-ui preset_picker` 及 workspace 全量 Rust 门禁通过。
- embedded 中英 README pairing 哈希已更新；使用 tsx loader 执行 pairing 校验，
  1073 对翻译文件一致；本项目 README 的实际 git blob hash 也分别精确匹配
  `README.i18n.yaml` 的两条记录。
- 主升级的 TypeScript、真实 Harness 与 PTY 门禁均通过；浏览器像素门禁的外部
  阻塞详见主记录，不影响本范围的 ID 与双语合同结论。
