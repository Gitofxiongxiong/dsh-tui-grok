# Bin Clippy 收尾

> 记录时间：2026-08-29 08:21:53 -0400
> 操作者/agent：Codex
> 状态：partial（workspace Clippy 全绿；完整 E2E 暴露 PTY 退出帧竞态）

## 目标与背景

Rust 1.98 workspace all-features Clippy 在核心与 Grok UI 两批兼容完成后，只剩 `dsh-pager-bin/src/main.rs` 中 1 处 `collapsible_if`。本记录将其做等价语法折叠，并执行完整本地门禁。

## 计划修改范围

- `docs/开发进度记录/2026-08-29_08-21-53_Bin-Clippy收尾.md`
- `crates/dsh-pager-bin/src/main.rs`

不在范围内：不改变 server/program/arguments 的优先级与错误处理，不改 Grok revision、vendored source、protocol 或真实 Harness。

## 风险、回滚和依赖

- let-chain 保持 `program.is_none()` 对环境变量解析的短路顺序。
- 回滚本记录对应提交即可；无数据迁移。
- 依赖 Rust 1.98.0、Node、Python PTY 环境和已锁定的 Grok checkout。

## 实际修改

- `crates/dsh-pager-bin/src/main.rs`：将 `program.is_none()` 与 `tui_server` 解包合并为 let-chain，保持环境命令解析和参数 fallback 语义。

## 验证结果

- `cargo fmt --all -- --check`：通过。
- `python3 scripts/check-source-manifest.py`：通过，90 rows，零 drift。
- `cargo clippy --workspace --all-targets --all-features -- -D warnings`：通过。
- `bash scripts/e2e.sh`：协议、972-case parity、workspace Rust tests、doctests、Clippy 和 build 均通过；最终 full PTY 偶发在进程退出后尚未读完 PTY 尾帧时断言 terminal restore，单独立即复跑 `python3 scripts/pty-smoke.py --binary target/debug/dsh-pager --full` 通过。该测试驱动竞态另立记录处理。

## Git 提交

- commit message：`fix(build): finish Rust 1.98 clippy gate`
- Progress-Record trailer：`Progress-Record: docs/开发进度记录/2026-08-29_08-21-53_Bin-Clippy收尾.md`
- 暂存区审计：提交前以 `git diff --cached --name-only` 核对仅含本记录与 `crates/dsh-pager-bin/src/main.rs`。

## 未解决问题和下一步

- 修复 PTY 测试在 `waitpid` 报告退出后未排空剩余 master 输出的竞态，再完整复跑 E2E。
