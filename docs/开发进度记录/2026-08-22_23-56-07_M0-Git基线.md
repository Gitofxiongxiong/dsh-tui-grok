# M0 Git 基线

> 记录时间：2026-08-22 23:56:07 +0800
> 操作者/agent：Codex `/root`
> 状态：completed

## 目标与背景

确认 successor 的 Git 根和可审计初始提交，使后续 M0/M1 工作满足一记录一 commit 规则。

## 设计契约和复用依据

- 对应长期计划章节：M0.1。
- 来源：当前 successor 工作树中的既有 docs、vendor、Cargo 和测试基线。
- 复用等级：不涉及 Grok 代码修改。

## 计划修改范围

- 文件：本记录文件；Git 元数据。
- 预期行为：仓库根为当前目录，默认分支为 `main`，初始提交纳入现有基线文件。
- 不在范围内：M0/M1 实现文件和进行中的 `2026-08-22_23-54-58_M0-M1治理与中性合约.md`。

## 风险、回滚和依赖

- 风险：当前目录没有 Git 历史，初始提交会记录现有工作树。
- 回滚：删除本地 Git 元数据需要用户明确授权，因此只建立可审计历史，不执行 destructive 操作。

## 实际修改

- 在 `/home/leo/code/dsh-pager-grok` 初始化 Git，默认分支为 `main`。
- 将当前 successor 既有文件和本记录纳入初始提交；排除下一批尚在进行的 M0/M1 实现记录。
- 仓库本地 Git 作者配置为 `Codex <codex@local>`；未修改全局配置。

## 验证结果

- `git rev-parse --show-toplevel`：指向 `/home/leo/code/dsh-pager-grok`。
- `git branch --show-current`：`main`。
- 第一次 commit 尝试因缺少本地作者身份失败，配置仓库本地身份后重试。

## Git 提交

- commit message：`chore: establish successor baseline`
- Progress-Record trailer：`docs/开发进度记录/2026-08-22_23-56-07_M0-Git基线.md`
- 暂存区审计：只包含初始 successor 基线和本记录，明确排除进行中的下一批记录。

## 未解决问题和下一步

继续 M0/M1 实现；初始历史无法还原 successor 创建之前的来源提交，只以 source manifest 和现有记录追溯。
