# Agent Preset 真实 DSH 浏览器联调

> 记录时间：2026-08-27 17:08:15 +0800
> 操作者/agent：Grok
> 状态：completed

## 目标与背景

上一批已把 `standard` / `code` / `minimal` / `cordis` 接到 grok TUI。本次不改
产品代码，只把当前 worktree 链到真实 DeepSeek Harness，经 `ttyd + xterm.js +
Playwright` 打开空白会话，验证欢迎页人设、`/preset` picker、空白切换和
`/preset status`。

## 设计契约和复用依据

- 对应长期计划：AGENTS.md 浏览器终端截图对比门禁。
- 复用等级：不适用（验证，不新增产品代码）。
- DSH 真源：真实 `apps/cli/lib/bin.js --profile …`，不改 Harness checkout。
- Grok 基准：本机 `/home/leo/.grok/bin/grok`。Agent preset 是 DSH 产品面，
  Grok 没有对等人设 picker；仍截空会话几何作对照，差异记为预期产品差异。
- 截图默认放工作区外，不入库、不含凭据。

## 计划修改范围

- 文件：
  - `docs/开发进度记录/2026-08-27_17-08-15_AgentPreset真实DSH浏览器联调.md`
- 预期行为：
  - 真实 backend hello 成功。
  - 空白欢迎页出现 `preset` / 标准模式或名册显示名。
  - `/preset` 打开名单，至少含 standard/code/minimal/cordis。
  - 空白会话选中另一人设后状态行出现 `Preset →`。
  - `/preset status` 报告 blank, switchable。
- 不在范围内：改 Rust/TS、提交真实长 prompt、入库截图、preset 创作台。

## 风险、回滚和依赖

- 风险：真实 LLM/API 会产生费用。本次默认不提交模型 prompt，只验证名单与
  空白切换。若握手后补一条极短 prompt 验证锁定，会单独记录。
- 风险：开发 profile 若未装上 `dsh-cordis-host-runner`，创造模式会挂起。
  缓解：专用 profile 重新 `plugin add`。
- 回滚：删除本记录即可；不回滚产品代码。
- 依赖：本机 `DEEPSEEK_API_KEY`、`ttyd 1.7.7`、Playwright Chromium、
  `/home/leo/aidreamschool/deepseek-harness`。

## 实际修改

- 仅本进度记录。产品代码未改。
- 专用 profile：`dsh-pager-grok-preset-browser`，link 当前 worktree 四包，并
  `pnpm add` `@deepseek-ai/dsh-agent-presets` 与
  `@deepseek-ai/dsh-cordis-host-runner` 0.1.0-rc.8（profile 根目录解析插件名）。
- 截图在工作区外：`/tmp/dsh-pager-preset-browser/`。

## 验证结果

Handshake：

```text
DSH_HARNESS_ROOT=/home/leo/aidreamschool/deepseek-harness
DSH_TUI_PROFILE=dsh-pager-grok-preset-browser
scripts/start-new-chat.sh --check --skip-setup
dsh-pager: tui.hello ok … server=deepseek-harness-tui/0.1.0
```

浏览器链路：`ttyd 1.7.7` → xterm.js → Playwright 1.61.1 Chromium；
1200×800、DPR 1、DejaVu Sans Mono 16、`TERM=xterm-256color`、
`127.0.0.1:7691`。真实 backend 为
`node …/deepseek-harness/apps/cli/lib/bin.js --profile dsh-pager-grok-preset-browser`，
`--new` 空白会话，cwd `/tmp/dsh-pager-preset-cwd`。未提交模型 prompt。

| 截图 | 观察到的行为 |
| --- | --- |
| `01-welcome.png` | 鲸鱼欢迎页；`preset 标准模式`；Tip `/preset changes agent · Shift+Tab plan/sandbox`；prompt 右下 `deepseek · 标准模式 · danger-full-access` |
| `02-preset-picker.png` | `/preset` 打开 Grok modal；四行：标准模式 default、PTC 模式、极简模式、创造模式，带官方中文描述 |
| `03-after-select.png` | 方向键选 PTC 后欢迎页与 prompt chip 变为 `PTC 模式`；状态行 `Preset → PTC 模式` |
| `04-preset-status.png` | `/preset status` → `Preset: PTC 模式 (blank, switchable)` |
| `05-grok-empty.png` | 同尺寸 Grok 1.0.5 空会话 splash；无 Agent preset picker（预期产品差异） |

console/page error 文件未产生。`result.json` 无 errors。

Grok 对照：空会话是 worktree/resume splash 与 `always-approve` chip，没有
四份 DSH 人设。几何上双方都是深色底、底部圆角 prompt、右侧 model/mode 标签；
人设名单是 DSH 产品面，不能做逐像素对等。

`danger-full-access` 是 Shift+Tab 权限循环，不是人设；本轮未改该循环。

## 阻塞与下一步

- 未跑真实 LLM prompt，因此没有验证开跑后 `agent-preset-locked`。空白切换已在
  真实 Host `agentPreset.select` 上通过。
- 名册异步到达前欢迎页可能短暂显示 `host`，截图时已收敛为 `标准模式`。
- 开发 profile `dsh-pager-grok-dev` 仍是 8 月 25 日的 link，若日常使用需重新
  `plugin add` / 安装 preset 包。

