# 迁移主分支测试合同

> 记录时间：2026-08-29 23:52:01 +0800
> 操作者/agent：Codex
> 状态：completed

## 目标与背景

用户要求把 `/home/leo/code/dsh-pager-grok` 的测试迁入当前
`upgrade/harness-0.1.2-alpha.1` 分支并全部运行。两个目录的共同基线是
`b4ce04b`；主分支此后只有 `00d93ba` README 变更，没有新增测试提交。因此本批次
不能用主分支 rc.8 测试直接覆盖已经适配 Harness `0.1.2-alpha.1` 的测试，而应审计
原测试合同，并把升级时退役或折叠后仍有价值的回归场景移植到新 bridge seam。

## 设计契约和复用依据

- 对应长期计划章节：Harness compatibility boundary、测试与验收门禁、稳定 identity / generation / effect。
- 主目录只读；不修改 `/home/leo/code/dsh-pager-grok`。
- Grok source path / commit / SOURCE_REV：本批次不涉及 Grok 源码或 UI 实现。
- 复用等级：C；测试覆盖 DeepSeek Harness bridge、协议和 control-plane 真源。
- 保留 alpha 分支现有 bridge/controller 测试，不恢复已删除的 rc.8 ApiProxy 或 cold projection recovery 产品包。
- 将旧测试的仍有效行为改写为 `TuiHarnessBridge` / `TuiGateway` 合同测试：冷会话不激活 Agent、命令目录/执行、协议版本、请求取消、跨 session 过滤与 transport 错误映射。

## 开始前审计

- 主目录：`main` / `00d93ba`，clean。
- 当前目录：`upgrade/harness-0.1.2-alpha.1` / `cad2580`，clean。
- 分叉点以后主分支没有 test/spec/scripts 测试相关提交。
- tracked 测试路径：主目录 38，升级目录 37。
- 主目录独有：旧 `dsh-tui-session-projection-recovery` 包的 2 个测试。
- 升级目录独有：`packages/dsh-tui-server/tests/bridge.spec.ts`。
- 35 个共同测试路径仍存在；其中 7 个测试文件为 alpha API 适配版本，其余保持一致。
- Rust inline test 函数名集合没有主目录独有项。

## 计划修改范围

- `packages/dsh-tui-server/tests/bridge.spec.ts`
  - 补充主分支 command/file-reference 与 cold recovery 合同在 alpha bridge 上的等价覆盖。
- `packages/dsh-tui-server/tests/gateway.spec.ts`
  - 补充协议版本、连接级请求取消、control payload/filter 和 transport error 合同。
- `docs/开发进度记录/2026-08-29_23-52-01_迁移主分支测试合同.md`
  - 记录范围、迁移映射、完整测试结果和提交审计。
- 不在范围内：生产代码、旧 recovery 包、package manifests、lockfile、fixtures、vendor、README 和历史记录。
- 若新增测试暴露产品缺陷，先创建新的进度记录再修改生产代码。

## 风险、回滚和依赖

- 直接恢复旧测试会重新引入已删除的 `dsh-host-apiproxy` 类型和 rc.8 profile，产生错误的回退压力；本批次只迁移行为合同。
- 异步取消/stream 测试可能因未等待状态就绪而产生时序抖动；使用显式 started barrier 和 AbortSignal。
- 完整 E2E 依赖已构建的独立 `/home/leo/code/deepseek-harness-latest`。
- 回滚：revert 本记录对应的单个提交即可删除新增测试覆盖；不涉及产品状态迁移。

## 预期验证

- 定向运行 `dsh-tui-server` 全部测试及 typecheck。
- `pnpm run verify:ts`。
- `scripts/e2e.sh`，含 Rust fmt/check/test/clippy、fixture、semantic parity 和 mock PTY。
- 显式 `cargo check/test --workspace --all-targets --locked`。
- ESC/stream PTY 脚本和隔离 profile 的真实 Harness E2E。
- 本批次无用户可见 TUI 变更；既有浏览器视觉证据不失效，不新增视觉门禁。

## 实际修改

- `packages/dsh-tui-server/tests/bridge.spec.ts`
  - 新增 projection-free cold row 保留、cold inspect 取消以及不激活 Agent 的 alpha 等价合同。
  - 扩展 command seam：缺失 `agentId`、命令成功匹配和未匹配返回值；保留原 file-reference 覆盖。
- `packages/dsh-tui-server/tests/gateway.spec.ts`
  - 新增 protocol mismatch 和无效 control payload 拒绝测试。
  - 新增连接 dispose 中止执行中 bridge request 的测试，使用显式 started barrier。
  - 扩展 session scope 过滤，确认其他 session 的 host status 不泄漏。
  - 新增 approval/question legacy payload 映射测试。
  - 扩展 all-session control stream，确认未 attach session 的 queue 仍进入 store 和通知。
  - 新增无 handler `-32601` 与 transport start/close 幂等测试。
  - 扩展 `TuiRpcError` 和非 Error throw 的 JSON-RPC 错误映射测试。
- 没有恢复已退役的 rc.8 recovery 包：其本地 fold/wrapper/dispose 行为在 alpha 中不再由本仓库拥有；等价用户合同由官方 session controller 加 bridge 的新测试覆盖。
- 没有修改生产代码、manifest、lockfile、fixture 或 vendor。

## 验证结果

- `pnpm --filter @dsh-pager-grok/tui-server typecheck`：通过。
- `pnpm --filter @dsh-pager-grok/tui-server test`：5 files、41 tests 全部通过；升级前为 36 tests。
- 定向测试首次运行有 1 条测试断言失败：取消已成功，但测试错误要求 `TuiGateway.dispose()` 同时 dispose bridge。bridge 生命周期实际属于外层 `serve()`；删除越界断言后复跑 41/41 通过，生产代码无需修改。
- `pnpm run verify:ts`：通过。
  - protocol 16、server 41、embedded 3、runtime 1、CLI 11，共 72 tests。
  - build、typecheck、runtime/CLI pack 和 5 个 native packages matrix 全部通过。
- `scripts/e2e.sh`：通过。
  - protocol fixtures 2 份同步；source manifest 90 行，local/upstream drift 0，license 缺失 0。
  - semantic parity 972 cases / 8 fixtures 通过。
  - Rust fmt、workspace check/test、all-targets/all-features clippy `-D warnings`、build、mock PTY 全部通过。
- `cargo check --workspace --all-targets --locked`：通过。
- `cargo test --workspace --all-targets --locked`：通过，0 failure。
- `python3 scripts/pty-smoke.py --binary target/debug/dsh-pager --full --timeout 45`：通过。
- `python3 scripts/pty-esc-parity.py --binary target/debug/dsh-pager`：通过。
- `python3 scripts/pty-stream-scroll.py --binary target/debug/dsh-pager`：通过；`marker=235 row=14->15, down_reports=24, tail=30->65`。
- 真实 Harness E2E：通过。

  ```bash
  DSH_HOME=/tmp/dsh-main-test-e2e.GgyTdp \
    DSH_HARNESS_ROOT=/home/leo/code/deepseek-harness-latest \
    DSH_TUI_PROFILE=dsh-pager-grok-main-test-port \
    DSH_TUI_INSTALL_LOCAL=1 scripts/real-e2e.sh
  ```

  hello/list/dashboard/load/PTY 全部通过；隔离 session
  `session-21c19311-0293-4c74-9dd5-1d6fc3a86087` 收到 `SessionLoaded`，未发送模型 prompt。
- 本批次只有测试变更，不改变 TUI 像素、布局或交互实现；上一提交
  `cad2580` 的 ttyd/xterm.js/Playwright 视觉证据继续适用，无需重复视觉截图。

## Git 提交

- 计划 commit message：`test: port main branch compatibility coverage`。
- Progress-Record trailer：`Progress-Record: docs/开发进度记录/2026-08-29_23-52-01_迁移主分支测试合同.md`。
- 暂存区审计：提交前确认只包含本记录和列出的 2 个测试文件，`git diff --cached --check` 通过。

## 未解决问题和下一步

- 没有未解决的测试失败或产品缺陷。
- `/home/leo/code/dsh-pager-grok` 保持只读且 clean；当前目录的 Harness alpha 适配测试没有被旧 rc.8 文件覆盖。
