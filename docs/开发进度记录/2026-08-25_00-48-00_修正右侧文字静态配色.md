# 修正右侧文字静态配色

- 时间：2026-08-25 00:48（Asia/Shanghai）
- 状态：已完成
- 目标：修正 thinking/tool 运行态渲染，使左侧竖条独立使用行波动画；右侧文字只在完成/未选中的灰色与运行/选中的白色之间切换，不随 tick 波动。
- 修改范围：
  - `crates/dsh-pager-grok-ui/src/views/transcript.rs`
  - 对应 transcript 配色测试
  - 本进度记录
- 验证范围：Rust 单测、workspace/clippy/format、PTY/E2E、ttyd + xterm.js + Playwright 浏览器截图。

## 进度

- [x] 确认问题：`apply_dynamic_accent` 当前把运行态内容 spans 也染成了 wave color。
- [x] 将 rail wave 与右侧文字 foreground 解耦：只有左侧 prefix 使用 wave color，右侧 spans 使用静态灰/白。
- [x] 增加“rail 随 tick 变化、文字不随 tick 变化”的回归测试。
- [x] 完成全量验证并更新记录。

## 实现结果

- 运行态：左侧 rail 继续按 `wave_brightness(tick, row)` 行波；右侧 thinking/tool 标题和内容固定为 `theme.text_primary`。
- 选中态：左侧 rail 和右侧文字都固定为白色，不受 tick 影响。
- 完成态：右侧文字保持灰色；失败状态继续保留错误红色。

## 验证结果

- `cargo test --workspace --locked`：通过。
- `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings`：通过。
- `cargo fmt --all -- --check`、`git diff --check`：通过。
- `bash scripts/e2e.sh`：通过。
- `REAL_E2E_TIMEOUT=60 DSH_TUI_INSTALL_LOCAL=1 ./scripts/real-e2e.sh`：通过。
- ttyd + xterm.js + Playwright：思考中、工具运行中、完成态和选中态均通过，控制台 `errors=[]`；截图目录：`/tmp/dsh-pager-grok-browser-20260825-colors/`。
