# 主分支接管前置审计

> 记录时间：2026-08-30 10:44:29 +0800
> 操作者/agent：Codex
> 状态：completed

## 目标与背景

- 用户明确要求把 `upgrade/harness-0.1.2-alpha.1` 提升为后续开发使用的
  `main`，并把当前 `main` 作为旧分支保留。
- 本记录固定接管前的精确提交、远端状态、内容收敛结论、验证证据和回滚点；
  后续实际远端 ref 结果使用新的审计记录收口，不事后改写本记录。
- 本批次只记录和验证，不修改产品、测试、workflow、版本或 package 内容。

## 设计契约和接管依据

- 两个目录是同一 Git 根的 worktree：旧目录检出 `main`，升级目录检出
  `upgrade/harness-0.1.2-alpha.1`。
- 旧 `main` 精确为 `e49c1267bc2177ac8f2019d1c04753c4992481bf`，且与
  `origin/main` 一致；升级分支精确为
  `f16f602070af9b0fe86742b7fac405e1f6b4bf33`，比远端升级分支领先 4 个提交。
- 共同分叉点为 `b4ce04b355e3cc32cd4b109d1a4608db9427129a`；升级分支
  已包含或以新 Harness 合同替代旧主分支的 README、绝对路径 prompt、Rust 1.98
  Clippy 和 PTY 尾帧修复，不再 cherry-pick/merge 旧实现。
- 接管使用远端归档分支
  `legacy/main-before-harness-0.1.2-alpha.1` 固定旧提交；只有确认归档 ref 和升级
  ref 均已到达远端后，才允许用精确
  `--force-with-lease=refs/heads/main:e49c1267bc2177ac8f2019d1c04753c4992481bf`
  更新 `origin/main`。
- GitHub 审计时默认分支是 `main`，无 branch protection/ruleset、无 open PR；
  接管后必须等待 `ci.yml`，失败时从 legacy ref 恢复，不删除任一安全分支。
- 本批次属于仓库治理，Grok source/revision、复用等级和产品 UI 不适用。

## 计划及实际修改范围

- `docs/开发进度记录/2026-08-30_10-44-29_主分支接管前置审计.md`
- 没有其他 tracked 修改；不切换或删除 worktree，不修改 package、lockfile、
  workflow、源码、fixture、vendor 或用户数据。

## 风险、回滚和依赖

- 风险：远端 `main` 在审计后被其他提交推进。处置：精确 lease 不匹配即停止，
  重新审计，绝不退化为无 lease 的 force push。
- 风险：升级分支 4 个本地提交尚未在 GitHub。处置：先推升级 ref并以
  `ls-remote` 校验，再创建旧主归档和接管。
- 风险：发布候选尚未经过 GitHub `main` CI。处置：接管后监控 CI；npm 发布
  另走版本分支、发布门禁、Tag 和 Trusted Publishing，不与 ref 接管混做。
- 回滚：`legacy/main-before-harness-0.1.2-alpha.1` 精确保存 `e49c126`；若接管
  后 CI 失败，可使用同样的精确 lease 把 `origin/main` 恢复到该 ref。

## 验证结果

- `pnpm run verify:ts`：通过；protocol 16、server 41、embedded 3、runtime 1、
  CLI 11，共 72 tests，并通过 runtime/CLI pack 与五平台 native manifest 审计。
- `scripts/e2e.sh`：通过；protocol fixtures 2、source manifest 90 rows 零漂移、
  semantic parity 972 cases/8 fixtures、workspace fmt/check/test/doctest、Rust 1.98
  all-targets/all-features Clippy、build 和 full PTY 全部通过。
- 使用 `/home/leo/code/deepseek-harness-latest` 的 tag
  `dsh-v0.1.2-alpha.1`、commit `cd5ef8148158c3a752a658978873241fdf8e2bbc`，
  隔离 `DSH_HOME=/tmp/dsh-main-promotion-e2e.QllAf0` 和 profile
  `dsh-pager-grok-main-promotion` 运行 `scripts/real-e2e.sh`：
  hello/list/dashboard/load/PTY 全部通过，未发送模型 prompt。
- 两个接管 worktree 在验证后均无 tracked/untracked 变更。

## Git 提交

- commit message：`docs(repo): audit Harness alpha main promotion`。
- Progress-Record trailer：`Progress-Record: docs/开发进度记录/2026-08-30_10-44-29_主分支接管前置审计.md`。
- 暂存区只允许包含本记录；实际远端 ref 与 CI 结果由接管后的独立记录收口。

## 未解决问题和下一步

- 依次推送升级 ref、创建并推送 legacy ref、精确 lease 接管 `main`，随后校验
  三个远端 ref 和 GitHub CI。
- 正式 npm 发布版本尚需用户明确；当前建议为 `0.2.0-alpha.1`、dist-tag
  `next`，发布五个 native 包、runtime 和 CLI。
