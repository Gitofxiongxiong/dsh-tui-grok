# 主分支闭合 Rust 1.98 Clippy 门禁

> 记录时间：2026-08-30 10:16:21 +0800
> 操作者/agent：Codex
> 状态：completed

## 目标与背景

- 用户要求解决主分支 `scripts/e2e.sh` 在 Rust 1.98 all-features Clippy
  的阻断，完整验证后 push 到 GitHub。
- 首个可见失败是 `dsh-pager-test-support/src/node_mock.rs` 的
  `io_other_error`；已通过的 alpha 分支审计记录证明，其后还会依次暴露
  host `collapsible_if`、vendored markdown wrapper lint、Grok UI lint 和 bin
  `collapsible_if`。
- 目标是一次闭合 workspace `-D warnings` 和完整 E2E，不修改业务行为、
  协议、Grok revision 或 upstream vendored Rust source。

## 设计契约和复用依据

- 已验证对照：Harness alpha 分支中 `修复本地测试门禁`、
  `Rust-1.98-Clippy兼容`、`Grok-UI-Clippy门禁`、`Bin-Clippy收尾` 四份
  记录及其当前通过的实现。
- 复用等级：C（DSH host/test/bin 真源的等价 Rust 1.98 语法改写）、
  B（本地 Grok UI wrapper 等价改写）、A1（vendored markdown Cargo wrapper
  和 `model.rs` module 的定点 lint allow）。
- nested `if` 只折叠为 let-chain/boolean-chain，保持短路、return、mutation、
  identity 和顺序。
- `Default` derive/测试结构体初始化/无效借用改写保持原值语义；
  高参数 helper 保持现有签名并只做定点 allow。
- 不修改 vendored markdown/model `.rs`；只在已是本地 wrapper 的
  `Cargo.toml`/module 边界 allow Rust 1.98 新 lint，并同步 source manifest。

## 计划修改范围

- `crates/dsh-pager-test-support/src/node_mock.rs`
- `crates/dsh-pager/src/control_plane.rs`
- `crates/dsh-pager/src/loader.rs`
- `crates/dsh-pager/src/presentation.rs`
- `crates/dsh-pager/src/session.rs`
- `crates/dsh-pager-grok-ui/vendor/grok/xai-grok-markdown/Cargo.toml`
- `crates/dsh-pager-grok-ui/src/agent_preset.rs`
- `crates/dsh-pager-grok-ui/src/views/preset_picker.rs`
- `crates/dsh-pager-grok-ui/src/runtime.rs`
- `crates/dsh-pager-grok-ui/src/slash.rs`
- `crates/dsh-pager-grok-ui/SOURCE_MANIFEST.md`
- `crates/dsh-pager-bin/src/main.rs`
- `docs/开发进度记录/2026-08-30_10-16-21_主分支闭合Rust-1.98-Clippy门禁.md`
- 不在范围内：协议、Harness bridge/profile、README、vendored `.rs`、
  fixture、lockfile、PTY 脚本和用户数据。如完整 E2E 另外暴露独立问题，
  在修改前新建记录。

## 风险、回滚和预期验证

- 风险：let-chain 误排序可能改变 duplicate/sequence/approval 收敛；按 alpha
  已验证窄 hunk 逐项对照，不整文件覆盖。
- 风险：主分支刚加入绝对路径修复，`slash.rs`/manifest 不能被 alpha
  旧哈希覆盖；修改后以主分支当前内容重算。
- 回滚：revert 本记录对应单个提交；无数据迁移。
- 验证：分 crate Clippy、source manifest、`scripts/e2e.sh`（含 protocol/source/
  972-case parity/workspace tests/all-features Clippy/build/full PTY）、`git diff --check`。
- push 前必须确认工作树干净、trailer/范围精确，并以显式
  `git push origin main` 推送。

## 实际修改

- `node_mock.rs` 使用 Rust 1.98 建议的 `io::Error::other`，闭合首个
  `clippy::io-other-error`。
- 在 `control_plane.rs`、`loader.rs`、`presentation.rs`、`session.rs`、
  `agent_preset.rs` 和 bin `main.rs` 中等价折叠 8 处嵌套条件，保留原有
  短路、返回值和副作用顺序。
- `preset_picker.rs` 改用 `Default` derive；`runtime.rs` 收紧测试初始化和
  无效借用，并只对现有高参数 helper 做定点 allow。
- `slash.rs` 只在 vendored `model` module 边界定点允许 Rust 1.98 新增的
  `if_same_then_else`；保留本分支已有的绝对路径分类修复。
- vendored markdown 仅在本地 Cargo wrapper 允许 `question_mark` 和
  `unnecessary_sort_by`，没有改动任何 upstream vendored `.rs`。
- 更新 `SOURCE_MANIFEST.md` 中 `slash.rs` 和 markdown wrapper 的本地哈希及
  变更说明；没有修改 Grok revision、协议、Harness profile 或产品行为。

## 验证结果

- `cargo fmt --all -- --check`：通过。
- `python3 scripts/check-source-manifest.py`：通过；90 rows，local/upstream
  drift 0，missing license 0。
- `cargo clippy --workspace --all-targets --all-features -- -D warnings`：通过。
- 第一次 `scripts/e2e.sh` 已通过 protocol/source/972-case parity/workspace
  test/all-features Clippy/build，随后在最终 PTY restore 断言暴露独立的
  退出尾帧竞态；该测试基础设施问题另由
  `2026-08-30_10-20-16_主分支排空PTY退出尾帧.md` 定界处理。
- PTY 修复后重新执行 `scripts/e2e.sh`：完整通过，最终输出
  `DSH/Grok M8-M10 end-to-end checks passed`。

## Git 提交与 push

- commit message：`fix(build): support Rust 1.98 clippy`。
- Progress-Record trailer：`Progress-Record: docs/开发进度记录/2026-08-30_10-16-21_主分支闭合Rust-1.98-Clippy门禁.md`。
- 暂存范围限定为计划中的 12 个实现/manifest 文件和本记录；PTY 脚本及其
  独立记录不混入本提交。
- push 目标：`origin/main`；提交后与独立 PTY 修复及此前绝对路径修复一起
  做 clean-worktree、trailer 和远端 HEAD 审计。

## 未解决问题和下一步

- 本记录范围内无未解决问题；继续完成独立 PTY 修复提交并统一推送。
